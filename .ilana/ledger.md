# MailX v0.23 Ìlànà Ledger

## 2026-09-21 | BOOT | conductor
Mode: FLEET. Process style: hybrid. Rigour: 3.
Scope: v0.23 structured logs, correlation, metrics, and health/readiness only. v0.24 excluded.
Intake: user directed Ìlànà to assume defaults; assumptions recorded as DEC-001..DEC-007.

## 2026-09-21 | G0 | conductor | GATE PASS
Evidence:
- milestone request defines purpose, boundaries, security constraints, validation, and completion criteria
- privacy/harm assumptions recorded in `.ilana/decisions.md`
- risks RSK-001..RSK-003 recorded in `.ilana/risks.md`
- ethics finding ETH-001 recorded with no halt
Decision: enter phase 01 requirements.

HANDOFF
  from: conductor
  to: analyst
  gate: G0 PASSED
  produced: .ilana/gates/G0.md, .ilana/decisions.md, .ilana/risks.md, .ilana/ethics.md
  open: none
  next: audit repository evidence and baseline testable v0.23 requirements

## 2026-09-21 | RECOVERY | documentarian | CR-002
Ìlànà had not been updated during MailX v0.1-v0.22 and `.ilana/` had never been committed (untracked; only the v0.23 BOOT/G0 artifacts existed).
Recovered MailX history and current architecture from Git (50 commits, `1218cf8`..`481da4a`) and code at HEAD `481da4a`.
Produced: `.ilana/milestones.md` (v0.1-v0.22 with evidence grades), `.ilana/architecture.md` (current architecture, 18 invariants, security, limitations, superseded designs),
DEC-008..DEC-032, RSK-004..RSK-011, DEF-001..DEF-003, MET-002..MET-006, CR-002, `state.json.mailx`. Existing v0.23 records (DEC-001..007, RSK-001..003, ETH-001, G0, SRS) preserved unchanged.
Permanent rule: `CLAUDE.md` (Ilana MUST be updated as part of every MailX task).
Uncertain/unavailable: v0.1-v0.6 numbering is inferred; v0.3/v0.4 split, and the version owning `3581def`/`57ee1d0`, are not recorded in the repo.
Roadmap (verified): v0.22 COMPLETE; v0.23 Logs/Observability NEXT (planning only, no code); v0.24 not started.
Not run: the Go test suite (documentation-only task).

## 2026-09-21 | G1 | analyst | GATE PASS
Evidence: SRS REQ-001..015, NFR-001..010, DOM-001..002 baselined (`docs/srs.md`); traceability rows exist for all; elicitation plan present; G0 passed.
Working tree verified: only `.ilana/`, `docs/`, `CLAUDE.md` untracked; no v0.23 production code; `origin/Feranmi_works` = HEAD `481da4a`.
Decision: enter phase 02.

## 2026-09-21 | G2 | architect | GATE PASS
Evidence: `docs/design-v0.23.md` DES-001..DES-008; components observe only (PostgreSQL/Redis remain truth); no queue.Queue interface change; env surface = 3 vars; no migrations.

## 2026-09-21 | G3 | interaction-designer | GATE PASS
Evidence: operator interface specified in `docs/design-v0.23.md` (endpoints, health bodies <1 KiB, bounded component names, log field contract, metric label sets).
Decision: enter phase 04 construction.

## 2026-09-21 | G4 | constructor | GATE PASS
Evidence: `internal/observability`, `internal/buildinfo`, hooks in smtp/worker/dispatch/webhook/api/queue, `cmd/mailx/observe.go`; 3 new env vars; no migrations; new production files all < 400 lines. Prometheus client_golang v1.24.1 (+ procfs, required for Linux builds; found when the Linux build failed).
Coding-standard note: existing `log.Printf` uses in cmd/ replaced by slog; SMTP sink no longer logs message content.

## 2026-09-21 | G5 | verifier | GATE PASS
Evidence: `gofmt` clean, `go vet`, `go build` (darwin + linux/amd64 + linux/arm64), `go test -count=1 ./...` and `go test -race -count=1 ./...` with explicit MAILX_TEST_DATABASE_URL and live Redis: all packages pass. New tests in observability, buildinfo, smtp, worker, dispatch, webhook, api, queue, cmd.
Docker Compose e2e (host-built linux/arm64 binary in an alpine image because the in-Docker `go mod download` stalled on network; repo Dockerfile updated with VERSION/COMMIT build args but not exercised end to end): normal live/ready/metrics; `/v1` absent on operator listener; PostgreSQL down => live 200, ready 503 `["postgres"]`, metrics 200; recovery => ready 200; Redis down => live 200, ready 503 `["redis"]`, `queue_depth_errors_total` increments; recovery => ready 200. Real API email, unauthenticated request and inbound SMTP message with marker strings: 0 markers in logs/metrics.
DEF-004 found and fixed during e2e (below).

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.23 complete pending commit. Not pushed. Known limits recorded in `architecture.md`. README redesign and `assets/readme/` were pre-existing user edits and are not part of this change.

## 2026-09-21 | REVIEW | constructor | CR-004
Greptile reviewed PR #14 (3 findings, DEF-005..DEF-007), all fixed with regression tests; `go vet`, `go build` (darwin, linux), `go test -race -count=1 ./...` with explicit DSN pass.
Note: architecture.md limitation 6 remains accurate; the SMTP-only mode now also serves the operator listener.

## 2026-09-21 | G0-G3 | conductor | v0.24 PLAN
Scope CR-005: outbound STARTTLS only. Reality check: HEAD was `a8849e0`, not the `4148ad6` in the request. RFC 3207 read (STARTTLS keyword, 220/454/501, state reset, EHLO after TLS, public servers must not require it). Design `docs/design-v0.24.md`. No migration needed.

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/smtp/client_tls.go` (307 lines), `client.go` refactor (hello/secure, raw/conn), stages, transfer TLS info, delivery fallback, `mailx_smtp_tls_sessions_total`, worker tls_* log fields, `cmd/mailx/tlsconfig.go`, Dockerfile ca-certificates, `.env.example`/compose. No InsecureSkipVerify anywhere in production code.

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64, linux/arm64), `go test -count=1 ./...` and `-race` with explicit PostgreSQL DSN and Redis: all pass, 0 skipped. TLS matrix A-M covered (client, delivery, retry, worker pipeline, observability privacy, cmd config); concurrency test repeated 15x and TLS subset 10x under -race, no leaks. Real Dockerfile build succeeded (exit 0); Compose e2e: health/readiness normal, PostgreSQL and Redis outage and recovery, invalid policy rejected at startup, `required` starts. DEF-008 found and fixed.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.24 complete pending commit. Not pushed. Next: v0.25 SMTP AUTH (not started).

## 2026-09-21 | G0-G3 | conductor | v0.25 PLAN
Scope CR-006: outbound SMTP AUTH to a trusted relay only. Baseline at `d0cbda6` green (fmt, vet, build, diff-check, test, race with real PostgreSQL/Redis). RFC 4954 read (mechanism list may change after STARTTLS; 535 permanent, 454 temporary; plaintext mechanisms need TLS; PLAIN over TLS mandatory; initial-response line limit). Design `docs/design-v0.25.md`. No migration.

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/smtp/client_auth.go`, `client.go`/`client_tls.go` integration, `delivery.Config.Relay` + `Result.Transport`, `transfer` Auth plumbing, `mailx_smtp_auth_attempts_total`, worker log fields, `cmd/mailx/relayconfig.go`, `smtptest` AUTH scripting. No InsecureSkipVerify; no credential in any print path.

## 2026-09-21 | G5 | verifier | GATE PASS
See final validation entry appended below when the full suite, Docker build and Compose checks complete.

## 2026-09-21 | G5 (final) | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64, linux/arm64), `go test -count=1 ./...` and `-race` with explicit PostgreSQL DSN and Redis: all pass, 0 skipped. AUTH subset and mixed-failure concurrency repeated 10x under -race with no leaks. Real Dockerfile build succeeded (exit 0). Compose: default start direct transport; relay config errors (password missing, host missing, bad port) fail startup naming variables only; a valid relay config starts and a secret marker never appears in container logs; liveness and readiness stay 200 with an unreachable relay; PostgreSQL and Redis outage/recovery unchanged. Security greps: no InsecureSkipVerify, no credential print path, no markers outside tests.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.25 complete pending commit. Not pushed (push planned after review). Next: v0.26 DKIM (not started). DEF-009 was caught before commit.

## 2026-09-21 | G0-G3 | conductor | v0.26 PLAN
Scope CR-007: DKIM signing, key lifecycle, verified-From. Baseline at `631836c` green (fmt, vet, build, diff-check, test, race with real PostgreSQL/Redis). RFC 6376 and RFC 8301 read (relaxed canonicalization, From must be signed, hash construction, rsa-sha1 forbidden, 2048-bit recommended). Design `docs/design-v0.26.md`; migration 000012 justified (key storage and lifecycle constraints need durable, tenant-safe schema).

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/dkim` (keys, canon, sign, service), `internal/secretbox`, migration 000012, `database/dkim_keys.go`, sender authorization + in-transaction recheck, API handlers/OpenAPI, cmd wiring, metric. No InsecureSkipVerify, no weak algorithm, no math/rand, no plaintext key path.

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64, linux/arm64), `go test -count=1 ./...` and `-race` with explicit PostgreSQL DSN and Redis: all pass, 0 skipped. Targeted DKIM/authorization/concurrency/rotation/deletion/delivery subset repeated 8x under -race. Independent DKIM verification (go-msgauth) of text, html, alternative, multipart/mixed+attachment; mutation and canonicalization tests; DB plaintext-marker inspection; capturing MX proves stored = transmitted bytes for direct and relay. Real Dockerfile build exit 0; Compose: migration 000012 applied, verified-From 403/202, DKIM create returns a pending TXT record with no private material, ciphertext row inspected, master-key validation (missing, malformed, same-as-webhook) fails startup naming variables only, valid key starts.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.26 complete pending commit. Not pushed. Next: v0.27 SPF (not started). DEF-001 and RSK-006 closed.

## 2026-09-21 | REVIEW | constructor | CR-008
Greptile reviewed PR #15 (3 findings, DEF-011..DEF-013), all fixed with regression tests; key generation is now preceded by the pending check and capped at 2 concurrent generations.

## 2026-09-21 | G0-G3 | conductor | v0.27 PLAN
Scope CR-009: SPF sending-authorization guidance. Baseline at `48939d0` green. RFC 7208 identity/evaluation rules applied (MAIL FROM vs HELO, single record, `all` ends evaluation, 10-lookup limit). Repository audit: MAIL FROM = From address, HELO = `mailx.local`. Design `docs/design-v0.27.md`. No migration: DNS is source of truth (DEC-059). Sending is not blocked by SPF (DEC-054).

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/spf` (record, sending, service), API `spf_handler.go` + routes + OpenAPI, `cmd/mailx/spfconfig.go`, metric `mailx_spf_verifications_total`, compose/.env.example variables (empty by default, no fake IPs).

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64), `go test -count=1 ./...` and `-race` with real PostgreSQL/Redis: all pass, 0 skipped (1229 passes under race). Parser fuzzed (FuzzParse, 50 s, no findings). SPF suite repeated 15x and API SPF tests 8x under -race. Real `docker compose build` exit 0; Compose runtime: default start healthy (live/ready), `MAILX_SENDING_IPS=127.0.0.1` refuses startup naming only the variable, `MAILX_SENDING_IPS=8.8.8.8` yields guidance record `v=spf1 ip4:8.8.8.8 ~all`, verify 409 on unverified, 404 unknown, health unaffected. No defects found in production code.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.27 complete pending commit. Not pushed. Next: v0.28 DMARC (not started).


## 2026-09-21 | G0-G3 | conductor | v0.28 PLAN
Scope CR-010: DMARC policy, alignment and readiness. Baseline at `cc8c178` green (23 packages, plain and race). RFC 9989 identified as the current standard (RFC 7489 obsolete). Source audit: MAIL FROM = From address, d= = From domain, HELO `mailx.local`. Real domain `appmd.dev` already publishes `v=DMARC1; p=none;` (Gmail reported dmarc=pass), used as a live existing-record case. Design `docs/design-v0.28.md`. No migration (DEC-065).

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/dmarc` (record, align, service), API `dmarc_handler.go` + routes + OpenAPI, adapters in `cmd/mailx/dmarcconfig.go`, metric `mailx_dmarc_verifications_total`. No new config.

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64), `go test -count=1 ./...` and `-race` with real PostgreSQL/Redis: all pass, 0 skipped (1293 passes under race). Parser fuzzed 30 s, no findings; dmarc suite 12x and API DMARC tests 6x under -race. Real `docker compose build` exit 0; Compose runtime against real `appmd.dev` DNS: existing `p=none;` preserved, DKIM and SPF paths ready, readiness ready, receiver_result not_observed; 409 for an unverified domain, 404 unknown; health unaffected; metric exposed. One test-authoring correction (MAIL FROM keeps brackets/case); no production defects.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.28 complete pending commit. Not pushed. RSK-020/RSK-021 (egress IP correctness, `mailx.local` HELO) remain OPEN. Next: v0.29 public SMTP identity (not started).

## 2026-09-21 | REVIEW | constructor | CR-011 v0.28 correction
Independent review found the DMARC Organizational Domain used the PSL (DEF-015, RSK-022). RFC 9989 sections 4.10, 4.10.1 and 4.10.2 re-read from the RFC text. Replaced by one bounded DNS Tree Walk that yields policy and Organizational Domain (`internal/dmarc/treewalk.go`, `align.go`); psd=y/psd=n implemented; multiple records discarded with the walk continuing yet reported as conflict; DNS errors fail safe (unknown, never a substituted parent policy). Evidence: RFC worked examples reproduced, PSL-vs-walk difference tests, policy/organization consistency test over 400 pseudo-random zones, walk bounded to 8 queries (RFC's 12-label example), parser and walk-input fuzzing, concurrency with psd cases, source scan forbidding PSL/suffix matching. Full suite plain and -race green with real PostgreSQL/Redis (1321 passes, 0 skipped), Docker build exit 0, read-only Compose run against real appmd.dev preserved its `p=none;` policy. RSK-022 closed; RSK-020, RSK-021, RSK-023 remain OPEN. No API shape change. A pre-existing test flake (DEF-016, a raw-substring assertion over the whole metrics dump) surfaced under -race and was fixed; `service.go` was split by responsibility into `service.go` (I/O) and `readiness.go` (pure model).

## 2026-09-21 | DECISION | conductor | deployment target
User chose an AWS Lightsail $7/month instance as the first deployment target (DEC-069); RSK-024 and RSK-025 recorded; the hosted-database question (Aiven free PostgreSQL) is open. Code fact verified: hosted TLS/password Redis is unsupported (architecture limitation 19). No code changed.

## 2026-09-21 | DECISION | conductor | single-host deployment
User approved DEC-070: everything on the one Lightsail $7 host, backups from day one, managed PostgreSQL only later. The earlier open hosted-database question in DEC-069 is closed. No code changed; the tuning, swap and backup steps are not yet written or implemented.

## 2026-09-21 | G0-G3 | conductor | v0.29 PLAN
Scope CR-012: public SMTP identity, PTR/rDNS readiness, EHLO and Message-ID. Baseline at `5d60bc4` green (24 packages, plain and race). Standards read from RFC text (5321 4.1.1.1/4.1.3, 7208 2.3/2.4, 1912 2.1, 5322 3.6.4) plus Gmail sender guidance (receiver practice, kept separate from RFC requirements). Audit found `mailx.local` in 4 production places (client EHLO, two Message-ID constructions, DSN Reporting-MTA). Design `docs/design-v0.29.md`; no migration.

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/smtpidentity` (hostname validation, PTR/FCrDNS readiness), `cmd/mailx/smtpidentity.go` (modes, `check-smtp-identity`), Message-ID helper and `api.Config.MessageIDDomain`, worker Reporting-MTA wiring, compose/.env.example.

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64), `go test -count=1 ./...` and `-race` with real PostgreSQL/Redis: all pass, 0 skipped (1392 passes under race). Hostname, PTR (A-L) and multi-IP matrices, failure injection, concurrency, three fuzzers, byte-capture EHLO tests (pre-TLS, post-TLS, HELO fallback, relay), Message-ID with independent DKIM verification, import boundaries. Real `docker compose build` exit 0; Compose runtime: local mode healthy, public mode healthy, IPs without hostname fails startup with a clear message, invalid hostname fails without echoing the value, `check-smtp-identity` works in the image; read-only live DNS check of the laptop IP reported `missing_ptr` (truthful). The user's already-running container was left untouched. RSK-021 closed; RSK-020 and RSK-023 remain OPEN.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.29 complete pending commit. Not pushed. OPERATIONAL NOTE: recreating the compose service with the existing `.env` (which declares `MAILX_SENDING_IPS`) now requires `MAILX_SMTP_HOSTNAME`. Next: v0.30 suppression (not started); the first controlled Internet-delivery test is a separate explicit action.

## 2026-09-21 | DECISION | conductor | low-memory PostgreSQL profile
User asked for the tested PostgreSQL settings as an opt-in profile (DEC-077). Added `compose.low-memory.yaml` (durability pinned on), `docs/low-memory-deployment.md`, startup database-settings validation (`database.DB.ServerSettings`, `cmd/mailx/dbsettings.go`) and tests. Evidence: Compose applies the exact settings (checked in `pg_settings`); MailX is silent against the profile and warns four ways against a weakened server; full suite green against a `max_connections=20` server (six full runs, no connection errors). One earlier first-run of the suite against a fresh tuned database reported 5 non-ok lines that could not be reproduced in six later runs (including fresh-database first runs with and without the tuning); cause unknown, no evidence it is related to the tuning. The default `compose.yaml` is unchanged.

## 2026-09-21 | G0-G3 | conductor | v0.30 PLAN
Scope CR-013: recipient suppression management. Baseline at `0b215ba` green (25 packages, plain and race). Traced one recipient: POST /v1/emails -> InsertMessage (recipients rows, outbox, queued event) -> queue -> worker.processOne -> coordinator/engine -> SMTP -> `PersistDeliveryOutcome` (attempt + status + event in one transaction) -> queue ack. Findings that shaped the design: outcomes persist BEFORE ack; RCPT is all-or-error; recipient rows existed but per-recipient status was never used; message status vocabulary and events had CHECK constraints. Research: RFC 3463 X.1.x/X.2.x/X.7.x read from the RFC text; RFC 5321 2.4 (local-part case) read; SES/SendGrid/Postmark suppression behavior as reference only. Design `docs/design-v0.30.md`; migration 000013 justified (durable policy state, tenant-safe, two query-driven indexes).

## 2026-09-21 | G4 | constructor | GATE PASS
`internal/suppression` (vocabulary, Normalize, QualifiesHardBounce), `database/suppressions.go` + migration 000013, `PersistDeliveryOutcome` atomic hard-bounce suppression, `worker/suppression.go` (`SuppressionGate`, enforcement before any transport), API handlers/routes/scopes/OpenAPI, acceptance check, metrics, `smtptest` RcptReplies/MailReply options.

## 2026-09-21 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64), `go test -count=1 ./...` and `-race` with real PostgreSQL/Redis: all pass, 0 skipped (1472 passes under race). Concurrency/stress suites repeated 8x under -race. Mutation check: disabling worker enforcement fails 8 of the new tests (they are not vacuous). Fuzzers: Normalize and the cursor decoder. Fake-MX connection counting proves zero SMTP for suppressed recipients (direct and relay), partial recipient filtering, suppression between retries, lookup/record failure deferral, ack-failure reclaim, cancellation, and the documented in-flight boundary. End to end with a real DB and the production adapter: hard bounce -> suppression -> later mail makes no connection -> unsuppress -> new mail attempted, old terminal messages never resurrect. Real `docker compose build` exit 0; a COPY of the real v0.29 dev database upgraded 12 -> 13 with all rows intact and the API/worker path exercised in throwaway containers (private Redis, non-resolvable `.invalid` domain, nothing sent); the real dev database and running container were untouched. Open risks RSK-020, RSK-023, RSK-026 unchanged (not resolved by this milestone). New risks RSK-027..030.

## 2026-09-21 | G6-G8 | release-manager | GATE PASS (local)
v0.30 complete pending commit. Not pushed. Next: v0.31 abuse controls (not started).

## 2026-09-22 | G0-G3 | conductor | v0.31 PLAN
Scope CR-014: outbound abuse controls (tenant/key request limits, recipient-volume limit, per-tenant queue cap, global backpressure, per-tenant/destination concurrency permits, fair dispatch, retry jitter). Baseline at `cd75bc6` green (26 packages, plain and race). Audit findings that shaped the design: `ListPendingOutbox` ordered globally by `available_at` (head-of-line starvation); Redis queue `Enqueue` blocks at capacity; idempotency claim precedes the FileStore write so a post-claim refusal must release it; suppression returns the deliverable count for charging; auth already fails closed on dependency errors (precedent). Research: RFC 6585 (429), RFC 9110 10.2.3 (Retry-After), GCRA/token-bucket references, RFC 5321 (100-recipient minimum), Redis Lua atomicity/Cluster hash tags. Design `docs/design-v0.31.md`; migration 000014 justified by EXPLAIN evidence.

## 2026-09-22 | G4 | constructor | GATE PASS
`internal/ratelimit` (GCRA `Allow`, lease permits, `Policy`), `internal/api/abuse.go` (middleware, `admitSend`, claim release), `database/abuse.go` + migration 000014 + fair `ListPendingOutbox`, `worker/permits.go`, `retry` jitter, `cmd/mailx/abuseconfig.go`, metrics, programmatic OpenAPI 429/503 with Retry-After, compose/.env docs.

## 2026-09-22 | G5 | verifier | GATE PASS
fmt, vet, builds (darwin, linux/amd64), `go test -count=1 ./...` and `-race` with real PostgreSQL and Redis: 27 packages, 1533 passes, 0 skipped. Mutation checks (8 deliberate breakages: claim release skipped, recipient cost 0, write fail-open, worker permits skipped, request limiter unwired, queue cap off, backpressure off, round robin broken) each fail tests. Concurrency: 40 parallel sends across 3 keys against a 10-token bucket accept exactly 10; two-process GCRA atomicity accepts exactly 100 of 400. Fuzzers: policy validation/Retry-After (10 s) and config loader (8 s) clean. Real `docker compose build` exit 0; a COPY of the dev database migrated 13 -> 14 (index swap verified) in throwaway containers on a private network (nothing sent; recipients `.invalid`), normal and low-memory profiles: 429 with Retry-After, queue cap 429 `tenant_queue_full` with the refused key reusable, invalid config exits 1 naming the variable, `off` warns loudly, limiter metrics bounded; the real dev containers and database were untouched. Defects DEF-020..022 (fixed). New risks RSK-031..034; RSK-029 remains open.

## 2026-09-22 | G6-G8 | release-manager | GATE PASS (local)
v0.31 complete pending commit. Not pushed. Next: v0.32 complaint/feedback loops (not started). OPERATIONAL NOTE: the running dev container still runs the v0.30 image; rebuilding it applies migration 000014 and turns the default limits ON (relevant to any script that sends fast).

## 2026-09-22 | G5 | verifier | PR #16 REVIEW FIXES
Greptile found 5 valid issues (DEF-023..027), all fixed with regression tests that each fail against the old code (mutation-checked). Full `go test -race ./...` with real PostgreSQL/Redis green. Architecture updated: queue cap counts retrying; permit holder is per claim; idempotency release is owner-guarded; every 503 carries Retry-After.

## 2026-09-22 | v0.32 | conductor/constructor/verifier | GATE PASS
Scope CR-015: outbound feedback (DSN bounces + complaints), no general inbound email. Audit: `StatusBounced`/`EventBounced` were reserved since v0.18, never produced — v0.32 activates EventBounced for feedback and adds recipient-level feedback columns rather than message-level, preserving the "never rewrite SMTP history" invariant (delivery_attempts/messages.status untouched). New `internal/feedback` (pure parser/classifier/correlator, fuzzed), migration 000015 (feedback table + recipients.feedback_* + events.complained), `database.ProcessFeedback` (one transaction, idempotent by raw-byte hash + state-transition guard), `POST /internal/feedback` (operator-token-authenticated, not tenant-scoped, not under /v1). Correlation: HMAC token over the existing 128-bit message ID; VERP Return-Path evaluated and deferred (RSK-036) to avoid a MAIL FROM/DNS change this milestone. Suppression reuses v0.30's narrow rule via new `suppression.QualifiesAsyncHardBounce` (5.1.1/5.1.6 only). Tests: DSN parsing (valid/malformed/oversized/excess groups), classification, correlation (valid/tampered/forged/wrong-secret/replay), persistence (dedup, transition-guard, cross-tenant isolation, concurrent duplicates), API auth/ingestion. Mutation-checked (4/4 catches). `go test -count=1 ./...` and `-race`: 28/28 packages pass. Roadmap updated: general inbound email removed pre-v1; v0.33 is Templates.

## 2026-09-22 | v0.33 | GATE PASS
Scope CR-016: reusable email templates. `internal/emailtemplate` (pure `{{name}}` renderer, fuzzed), migration 000016 (`templates` table + `templates:read/write` scope CHECK), `internal/database/templates.go`, `internal/api/template_handler.go` (CRUD), `POST /v1/emails` extended with `template_id`+`variables` (rendered once before MIME/DKIM, never at delivery/retry; idempotency fingerprint unaffected by later template edits). OpenAPI updated + drift-tested. `go test -count=1 ./...` and `-race`: 29/29 packages. Mutation-checked: render-skip and HTML-escape-removal both caught. Fresh-container migration smoke test: 000016 applies cleanly, prior v0.32 columns intact. RSK-029 and v0.32 deferred items (feedback metrics, VERP, complaint adapters) untouched.

## 2026-09-22 | v0.34 | GATE PASS
Scope CR-017: contacts. `internal/contact` (Normalize deliberately distinct from suppression.Normalize — local-part case preserved; ValidateAttributes/ValidateName; fuzzed), migration 000017 (`contacts` table, tenant+normalized_email UNIQUE, `contacts:read/write` scope CHECK), `internal/database/contacts.go`, `internal/api/contact_handler.go` (CRUD), OpenAPI + drift tests updated. `go test -count=1 ./...` and `-race`: 30/30 packages. Mutation-checked: tenant predicate removal, DB uniqueness removal, and accidental suppression-cascade-on-delete all correctly caught by tests. Fresh-container migration smoke test: 000017 applies cleanly, all prior tables intact. Suppression/contact independence tested explicitly in both directions. Sending never requires a contact (tested). RSK-029 and v0.32 deferred items untouched.

## 2026-09-22 | v0.35 | GATE PASS
Scope CR-018: audiences (group membership only, no sending). Migration 000018 (`audiences`, `audience_members` with composite tenant-integrity FKs, `audiences:read/write` scope CHECK), `internal/database/audiences.go`, `internal/api/audience_handler.go` (CRUD + membership), OpenAPI + drift tests updated. `go test -count=1 ./...` and `-race`: 30/30 packages. Mutation-checked: app-level tenant check removal, DB uniqueness removal, and contact-cascade removal all correctly caught by tests (one mutation targeting a redundant defense-in-depth FK path was inconclusive against its original test target but the underlying tenant-isolation invariant was independently confirmed by the app-check mutation). Fresh-container migration smoke test: 000018 applies cleanly, prior tables intact. Cascade/lifecycle and suppression independence tested in both directions (audience delete, contact delete). Fixed two pre-existing test assumptions (TestContactsMigrationRoundTrip, rollBackPastSuppressions) that assumed their own migration was the newest — same class as DEF-018/020, now more robust to future migrations. RSK-029 and v0.32 deferred items untouched.

## 2026-09-22 | v0.36 | GATE PASS
Scope CR-019: broadcasts/bulk sending, orchestration only over the existing send pipeline. Design decided before coding: two-phase (snapshot/materialize) bounded async expansion mirroring internal/dispatch's polling pattern; audience/template/personalization all snapshotted (PostgreSQL-clock watermark for audience, copied template source, copied contact attributes); broadcast_recipients.id doubles as messages.id for crash-idempotent materialization; suppression re-checked late per recipient; abuse controls shared with normal sends; HTTP idempotency reuses v0.20's exact pattern. Migration 000019 (broadcasts, broadcast_recipients, broadcasts:read/write scope CHECK). New package internal/broadcast (Expander). 31/31 packages pass go test -count=1 and -race. Crash-window analysis (durable per-phase state + ON CONFLICT DO NOTHING + message-existence idempotency check) verified by dedicated tests, not assumed. Mutation-checked: recipient uniqueness removal, suppression-check removal, template-snapshot bypass, audience-watermark removal, tenant-predicate removal, and cursor-checkpoint removal all correctly caught (two initially misconfigured mutations fixed and a dedicated frozen-template test added). Synthetic 100k-audience/50k-recipient EXPLAIN evidence in a throwaway container found and fixed a real partial-index design flaw (~115x fewer buffer reads after the fix). 600-contact end-to-end expander test proves bounded per-tick processing (never the whole audience in memory or one transaction) in ~4s. Docker/Compose migration smoke test: 000019 applies cleanly, all prior tables intact. Cancellation and broadcast DELETE/PATCH deliberately deferred (RSK-039). RSK-029, RSK-038, and v0.32 deferred items remain visible.

## 2026-09-22 | v0.37 | GATE PASS
Scope CR-020's target milestone: scheduled sending for email and Broadcasts, extending existing durable-acceptance machinery rather than building a second scheduler. Source-truth audit first: found normal-email `scheduled_at` already wired since v0.18 (`sendEmailRequest.ScheduledAt` -> `outbox.available_at`) but with no dedicated tests and a Go-clock (not DB-clock) due comparison in `ListPendingOutbox`. Fixed the clock authority (dropped the Go `now` parameter; `fairOutboxSQL` now compares against PostgreSQL's own `now()` throughout — same lesson v0.36 already learned from its acceptance-watermark skew). Added Broadcast scheduling by extending `ClaimActiveBroadcasts`'s existing runnable criteria with `send_at IS NULL OR send_at <= now()` — the SAME v0.36 expander, no new poller, no new status. Migration 000020 (`broadcasts.send_at` nullable TIMESTAMPTZ + `idx_broadcasts_active_due`). Snapshot decision made explicitly and tested: `audience_snapshot_at` stays acceptance-time regardless of `send_at`, so scheduling does not widen RSK-038 (proven, not assumed). Suppression/abuse controls confirmed unchanged at their existing late boundaries. `send_at`/`scheduled_at` participate in HTTP idempotency fingerprinting automatically (whole-request hash); tested that a changed schedule under the same key conflicts rather than rescheduling. No reschedule/cancel endpoint (deliberately deferred, consistent with v0.36's broadcast-configuration immutability). `go test -count=1 ./...` and `-race -count=1 ./...`: 32/32 packages pass. Mutation-checked: removing the broadcast due predicate and removing the outbox due predicate both correctly failed their respective no-early-send tests (confirmed, reverted). EXPLAIN evidence at 2003 active broadcasts (2000 future/3 due): bitmap index scan on the new partial index, 8 buffer hits, 0.018ms — no sequential scan. Docker Compose migration + runtime smoke test: 000020 applies cleanly against the live dev database, dispatch and broadcast-expansion components start without error against the new schema. RSK-029, RSK-038 (reconfirmed not enlarged), RSK-039 (unchanged), and v0.32 deferred items remain visible. Roadmap: v0.37 complete, v0.38 Advanced Analytics next.

## 2026-09-22 | PR review fix | GATE PASS
PR #17 (https://github.com/Ferousco-dev/Mailx/pull/17, v0.32-v0.37 bundle) opened for Greptile/CI review. Greptile flagged 6 findings (5 P1, 1 P2), all on code that predates this PR's own v0.37 diff but in scope on this branch: (1) broadcast recipient claiming (`PendingBroadcastRecipients`) released its SKIP LOCKED row lock as soon as the query returned, before Go-side render/sign/quota-charge/InsertMessage ran, so two truly-concurrent expander instances could both process the same recipient — fixed with an atomic claimed_at lease (migration 000021, `ClaimPendingBroadcastRecipients`); audited `ClaimActiveBroadcasts`/`SnapshotBroadcastBatch` and found them already individually safe, so only the recipient path needed the fix. (2) a deterministically-failing recipient retried forever, recharging quota every tick and blocking its broadcast from ever completing — fixed with a bounded `maxRecipientAttempts` (5) and a new terminal `failed` status. (3) email template lookup/rendering ran before checking for an already-completed idempotency replay, so a retry after the template was edited/deleted returned an error instead of replaying the original — fixed with an early `replayIfCompleted` check before rendering. (4) the broadcast handler claimed the idempotency key before validating audience/template/domain, so a correctable 404/403 left the claim stuck in_progress — fixed by reordering validation before the claim (matching handleSend's existing pattern). (5) `mergeVariables` only applied the recipient's own name/email when the key was absent from globals/attributes, so a broadcast-level "name"/"email" template variable silently overrode every recipient's personalization — fixed to apply the recipient's fields unconditionally, after globals. (6) `loadFeedbackConfig` silently disabled feedback ingestion if only one of its two required env vars was set — fixed to a fatal startup error. All 6 fixes have dedicated tests (including 2 new mutation-checked concurrency/failure-bounding tests and 1 new mergeVariables unit test), verified caught by mutation (temporarily reverting each fix reproduces the original test failure, then reverted). `go test -count=1 ./...` and `-race`: 32/32 packages pass. Docker Compose migration (000021) + runtime restart smoke test: clean, no errors. Not pushed/committed yet at time of this entry — see git log for the actual commit.

## 2026-09-22 | PR review fix, round 2 | GATE PASS
Greptile re-reviewed the fix commit (4465bcd) itself and flagged one new P1: `RecordBroadcastRecipientFailure` counted EVERY materializeOne error uniformly toward the terminal-failure budget, including transient ones (DKIM key lookup, filesystem, database) — meaning 5 unlucky transient failures could permanently drop a recipient a later tick would have delivered. Fixed by introducing `errDeterministic`, wrapping only render/MIME-build/parse errors (which fail the same way on every retry given the same frozen inputs) with it; DKIM/filesystem/DB errors are left unwrapped and now retry indefinitely, never consuming the bounded counter. Added `TestExpanderTransientFailureNeverTerminates` (fails FileStore.Save 8 times, asserts status stays 'pending' and attempts stays 0) alongside the existing `TestExpanderOneBadRecipientDoesNotAbortBatch` (still correctly counts as deterministic via outbound.Build's address validation). `go test -count=1 ./...` and `-race`: 32/32 packages pass (one isolated `-race` flake on an unrelated pre-existing test reproduced as a pass on 2 immediate reruns — connection-pool contention under load, not a real failure). Not yet committed at time of this entry.

## 2026-09-22 | PR review fix, round 3 | GATE PASS
Greptile re-reviewed the round-2 fix (9ebdd0e) and flagged 2 more P1s, both in the same errDeterministic/transient split introduced in round 2: (1) DKIM SignMessage errors were ALL treated as transient, but SignError distinguishes outcomes (key_unavailable = genuinely transient DB lookup; key_decrypt_failed/key_invalid/domain_mismatch/sign_failed = permanent given the domain's stored key state) — fixed with classifyDKIMError, splitting by Outcome. (2) FileStore.Save's "record already exists" error (when a prior attempt saved the file but then crashed/failed before InsertMessage) was treated as an ordinary failure, but it recurs identically on every future retry for that same recipient id — permanently blocking it. Added storage.ErrRecordExists and made materializeOne treat it as idempotent success (skip re-save, proceed to InsertMessage), consistent with the existing message-existence idempotency check already used elsewhere in the same function. Both fixes have dedicated tests (TestClassifyDKIMErrorSplitsByOutcome, a pure unit test of the classification logic; TestMaterializeOneTreatsExistingFileRecordAsSuccess), both mutation-verified (temporarily reverting each fix reproduces the original failure, confirmed, reverted). `go test -count=1 ./...` and `-race`: 32/32 packages pass. Not yet committed at time of this entry.

## 2026-09-22 | ad-hoc revision | GATE PASS
User pushed back on the just-shipped opt-in `priority: "urgent"` tier (previous ledger entry): requiring every caller to remember to flag time-critical mail is extra stress on MailX's users, and pointed to Resend's documented front-loaded webhook-retry schedule as the better pattern (clarified: that specific schedule is for webhook callbacks, not SMTP delivery retries — no directly comparable Resend number existed, but the FRONT-LOADED PATTERN itself was the right takeaway). Reverted the urgent-priority commit cleanly (`git revert`, all associated migration/API/worker/storage/test changes removed together) and replaced `DefaultBackoffPolicy` with one universal front-loaded schedule (5s/5min/30min/2h/5h) applied to every send automatically, no field, no flag. Added `BackoffPolicy.Schedule []time.Duration` to represent the irregular (non-fixed-ratio) sequence. Updated every test that hardcoded the old default's specific delay values across `internal/retry`, `internal/queue`, `internal/worker`. `go test -count=1 ./...` and `-race`: 32/32 packages pass. Mutation-checked: disabling the Schedule branch correctly fails TestDefaultBackoffPolicy. Docker Compose build + runtime restart smoke test: clean (no schema/API change this round — pure retry-policy logic). Manually retested delivery to feranmioresajo@icloud.com mid-session under the (now-reverted) urgent tier and observed the fast retry cadence firsthand (~10s/~20s instead of 30min) before the design was revised to the universal default. Not yet committed at time of this entry.

## 2026-09-22 | PR review + CI fix | GATE PASS
Two issues found checking PR #17's CI on commit d421615: (1) Validate Commit Policy FAILED — the earlier `git revert`'s default message ("Revert \"feat: ...\"") does not follow this repo's Conventional Commits policy. Fixed by reconstructing the branch (cherry-pick onto a fresh branch from the pre-revert commit, reworded revert commit as "revert: remove urgent send priority tier", re-applied the front-loaded-backoff commit on top, force-pushed) rather than leaving a permanently-failing commit-policy check in history. (2) Greptile P1 on d421615: DefaultBackoffPolicy's 5-entry schedule had an unreachable 5th entry (5h) — with DefaultAttemptLimit at 5 operations, only 4 delays are ever scheduled, so real exhaustion was ~2h35m, not the documented ~7h35m. Fixed by trimming to exactly 4 entries (5s/5min/30min/2h) and updating the test to assert `len(Schedule) == MaxAttempts-1` so a future regression of this exact class fails loudly. Also fixed, same session, live debugging: `compose.yaml`'s mailx service now pins public DNS servers — Go's CGO_ENABLED=0 static build's pure-Go resolver was hitting spurious ~8s timeouts against Docker's embedded DNS proxy for external MX lookups (reproduced live against icloud.com), unrelated to any MailX code defect; confirmed fixed by a live retest that got all the way to iCloud's SMTP server (TLS 1.3 established) before iCloud rejected on an unrelated Spamhaus SBL IP-reputation block (external network issue, not fixable in code). `go test -count=1 ./...` and `-race`: 32/32 packages pass. Mutation-checked: reintroducing the unreachable 5th entry correctly fails the updated test. Not yet committed/pushed at time of this entry.

## 2026-09-22 | PR review, round 4 | GATE PASS
Greptile's cumulative PR summary flagged 3 outstanding P1s as blocking merge-readiness. Triaged: Issue 3 ("equivalent priorities conflict") was STALE — referenced the `priority` field from the urgent-priority tier already reverted 2 rounds ago; no action needed, will clear on next re-scan. Issues 1 and 2 were real and fixed: (1) "Broadcast claims release early" — `ClaimActiveBroadcasts` had the same unprotected-SKIP-LOCKED bug already fixed for recipients (DEC-134); attempted the identical atomic-lease fix, but it broke `TestExpanderBoundedAcrossManyTicksForLargerAudience` (a broadcast is legitimately re-claimed by the same instance every tick across many ticks, unlike a recipient claimed once per materialization — a multi-minute lease blocks the owner from ever re-claiming its own in-progress broadcast). Reverted the lease fix after confirming (and now documenting explicitly, DEC-147) that the original RSK-040 reasoning was correct: every downstream operation is already individually guarded, so the residual risk is wasted redundant work under genuine concurrent overlap, never a duplicate send/charge. (2) "Incomplete records are queued" — `storage.FileStore.Save` now self-heals a crash-partial message directory instead of treating `ErrRecordExists` as blanket success; guarded by a 5-second age check after the completeness-check-alone version corrupted `TestFileStoreSaveConcurrentSameID`'s "exactly one concurrent Save wins" invariant (caught immediately, confirmed via 10x mutation reruns: 8/10 fail without the guard, 10/10 pass with it). `go test -count=1 ./...` and `-race`: 32/32 packages pass. Docker Compose build + runtime restart smoke test: clean (no schema change this round). Not yet committed at time of this entry.

## 2026-09-22 | v0.38 | GATE PASS
Confirmed v0.37's bug-fix pass clean before starting: HEAD e11322a, clean tree, all CI/Greptile checks green on GitHub, full local `go test -count=1 ./...` and `-race` both 32/32 packages passing. Scope CR-023: read-only tenant-facing sending analytics over EXISTING durable facts (`events` table), no second delivery-truth source, no OLAP infrastructure. Design-first per the spec: precise metric vocabulary documented before any query was written (`internal/database/analytics.go`'s package doc) — "delivered" is SMTP acceptance only, never inbox placement; facts are not mutually exclusive (an accepted message later bounced counts as both); CurrentlySuppressed is current-state, not historical. Three endpoints (overview/timeseries/broadcasts/{id}), `analytics:read` scope (migration 000022 — the ONLY schema change, adding the scope to the CHECK constraint; zero new tables, zero new indexes since `idx_events_tenant_occurred` already covered every query). `[from,to)` half-open, 90-day range cap, 744-bucket cap rejected analytically before query execution. Broadcast analytics proven to use the durable `broadcast_recipients` snapshot, never live Audience membership (mutation-caught test). Open/click tracking deliberately deferred per the spec's own strong guidance (privacy/false-positive concerns), not silently added. `go test -count=1 ./...` and `-race`: 32/32 packages pass. Mutation-checked: tenant-predicate removal, half-open-boundary-to-inclusive, bucket-bound removal, and live-Audience-join-instead-of-snapshot all correctly caught (4/4), reverted. EXPLAIN evidence at 50k synthetic events / 90-day spread with a selective 1-day query window: index scan via idx_events_tenant_occurred, 17 buffer hits, <0.15ms (both overview and timeseries). OpenAPI updated (3 new paths + 4 new schemas) and drift-tested (TestOpenAPIRoutesMatchRuntime passes). Docker Compose migration + runtime restart smoke test: clean. Does not close RSK-035 (feedback-ingestion operator metrics — a distinct concern). Not yet committed at time of this entry (per this milestone's explicit "do not push" instruction, committing locally only).

## 2026-09-22 | v0.39 | GATE PASS
Confirmed v0.38 complete before starting (HEAD 85781c7, clean tree, `go build`/`vet`/`test -count=1 ./...` all passing). Source-truth audit found NO existing source-IP binding despite `MAILX_SENDING_IPS` already existing (used only for SPF guidance text) — added `smtp.ClientConfig.SourceIP` + `net.Dialer.LocalAddr` wiring first, proven with a portable test (`TestClientSourceIPIsWiredIntoDialer`, using an unassigned TEST-NET-3 address to force a bind failure — the second-loopback-alias approach doesn't work on macOS). Found and fixed unrelated local dev-DB drift (`messages.priority`, residue of the reverted urgent-priority feature, no committed migration) by resetting the disposable Docker Postgres volume and re-migrating from zero to HEAD; confirmed via `git log`/migration-file audit that this was never committed schema, so nothing was lost. Design reviewed and REVISED twice by the user before implementation: (1) added per-attempt historical transport-identity snapshot columns (not just `sending_member_id`) since member config is mutable; (2) changed disabled-member semantics from a vague "kill switch" to an explicit no-failover HOLD (zero SMTP, preserve stickiness, bounded 30s re-check, resume on the same member). Migration 000023 (`sending_pools`, `sending_pool_members`, 3 routing columns), validated fresh-from-zero AND via a manual up/down/up round-trip against the disposable dev DB before any Go code was written. Implemented `internal/routing` (pure `SelectMember` + `Router`), DB layer (`internal/database/routing.go`, `InsertMessage`/`LoadDeliveryState`/`PersistDeliveryOutcome` extended), worker wiring (`holdIfMemberDisabled`), `cmd/mailx/serve.go`'s `buildRouter` (one Engine per direct member, relay members alias the existing base engine), and 7 operator-only CLI commands (`sendingpools.go`) — verified end-to-end against the real disposable dev DB (create pool → member → enable/disable → list). Two real bugs caught by this package's own DB-backed test suite once the native Postgres was reachable: pgx cannot scan `inet` into `*string` (fixed with `host(...)` casts) and the v0.39 columns broke this package's standing migration-upgrade regression tests (`TestWebhookMigrationUpgradesDurabilityPrerequisite` et al., which insert with current Go code against a schema missing the newest migration(s) — a rolling-deploy-safety invariant), fixed by making the new-column INSERT clauses conditional on actually having a value. `go build`/`vet`/`gofmt -l`: clean. `go test -count=1 ./...` and `-race ./...`: all packages pass (real Postgres + real Redis, both via the disposable Docker Compose stack). Docker Compose fresh build + full stack up + runtime log check: clean, server boots with zero pools configured (legacy path unchanged). No OpenAPI changes (no tenant-facing API added, by design — DEC-158). Ilana updated: milestones.md, architecture.md (new "Sending Pools / Routing" section), decisions.md (DEC-157..161), state.json counters/mailx block. Committed locally, not pushed (matching the prior two milestones' pattern; push only on explicit instruction).

## 2026-09-22 | v0.40 | GATE PASS
Confirmed v0.39 complete before starting (HEAD 3226e3d, clean tree, `go build`/`vet` clean). Researched current BIMI standards before writing any code (task-mandated): the BIMI Internet-Draft (draft-brand-indicators-for-message-identification) for TXT record ABNF/selector/DMARC-prerequisite/redirect-silence, and BIMI Group guidance for the SVG Tiny 1.2 Portable/Secure profile's security-relevant requirements. Found and resolved one standards conflict: some BIMI guidance (RFC-7489-era) states `pct=100` is required at `p=quarantine`, but MailX's own DMARC (RFC 9989, which removed `pct` entirely) already discards that tag — conservative resolution: BIMI's DMARC prerequisite is "effective policy != none", full stop (DEC-163). Built `internal/bimi` as a pure-derived, unpersisted package (no migration — DEC-162), reusing `internal/dmarc` as the DMARC fact source via the same Store/TXTResolver/DKIMState-style seam pattern, and `internal/webhook.URLPolicy`'s DNS-rebinding-safe DialContext for logo/certificate fetching rather than a second SSRF implementation (DEC-167). SVG validation is bounded and security-focused (no entity expansion, depth/node caps, banned script/foreignObject/image/style/external-ref elements) — explicitly NOT full SVG-Tiny-PS conformance certification (DEC-165). Certificate handling is structural x509 parsing only, no trust chain, no revocation, no private CA (DEC-166). Every API response carries a mandatory `disclaimer` field enforced at the OpenAPI schema level (required, not optional) stating MailX readiness never guarantees mailbox-provider display (DEC-168) — the task's hardest requirement. API: `GET/POST /v1/domains/{id}/bimi[/verify]`, asset validation opt-in via `?validate_assets=true` (adds up to 2 bounded HTTPS fetches, so never the default). `go build`/`vet`/`gofmt`: clean. `go test -count=1 ./...` and `-race ./...`: all packages pass (real Postgres + Redis via the disposable Docker Compose stack) — 39 internal/bimi tests covering record parsing, SVG structural validation (including a billion-laughs-shaped entity payload and 1000-deep nesting, both bounded safely), certificate parse/expiry, SSRF/redirect/size-limit fetch behavior, readiness state combinations, service-level DNS/DMARC/org-domain-fallback orchestration, and 4 API-level tests (tenant isolation, verified-domain requirement, no-DNS-on-describe, asset-fetch-is-opt-in). OpenAPI updated (2 new paths + BimiStatus schema) and drift-tested (`TestOpenAPIRoutesMatchRuntime` passes against the real runtime mux). Docker Compose rebuild + runtime restart smoke test: clean. Ilana updated: milestones.md, architecture.md (new "BIMI brand indicators" section), decisions.md (DEC-162..168), state.json. Committed locally, not pushed (matching the prior three milestones).

## 2026-09-22 | PR #18 review | GATE PASS
Greptile flagged 4 P1s and 1 P2 on PR #18 (v0.39+v0.40 combined). All valid, all fixed: (1) `routing.Router.Deliver` silently fell back to `Base` for a `MemberID` unknown to the startup-built registry (member added, or a hostname fixed, after the process started) — could dial the wrong source IP/EHLO identity or bypass a relay undetected; now returns a temporary `*delivery.Error` (new `routing.ErrUnknownMember`) so the message holds/retries instead (DEC-169). (2) `buildRouter` mapped every `relay`-kind pool member to `base` unconditionally — on a process running in pure direct mode (no `MAILX_RELAY_*`), a relay member would silently send direct, contradicting its declared kind; `buildRouter` now takes `relayConfigured bool` and omits relay members when no relay is actually configured, routing them into the same unknown-member hold path (DEC-170). (3) BIMI readiness ignored `CertInfo.CurrentlyValid` — a structurally parseable but expired/not-yet-valid certificate reported `ready`; fixed in `readiness.go` (DEC-171). (4) `bimi.Service.lookupOne` treated every DNS lookup error (SERVFAIL, timeout, genuine NXDOMAIN) identically as `not_configured`, which could both misreport a transient outage and incorrectly trigger the organizational-domain fallback; now uses the same `*net.DNSError` IsNotFound/IsTemporary/IsTimeout distinction `internal/spf`/`internal/dmarc` already use (DEC-172). (5, P2) the shared `AssetFetcher` was bounded to the 32KB logo limit for both logos and certificates, rejecting valid 32-64KB certificates before `ParseCertificate` ever saw them; bumped to `MaxCertificateBytes` (64KB), relying on `ValidateSVG`'s own tighter bound for logos (DEC-173). Added/updated tests for all 5 (`TestRouterUnknownMemberIDNeverUsesBase`, `TestReadinessCertificateIssueWhenExpired`, `TestServiceVerifyTransientDNSErrorIsUnknownNotNotConfigured`). `go build`/`vet`/`gofmt`: clean. `go test -count=1 ./...` and `-race ./...`: all packages pass (real Postgres + Redis). Docker Compose rebuild + boot smoke: clean. Ilana updated: architecture.md (Router/asset-fetching/DNS-error sections), decisions.md (DEC-169..173), state.json.

## 2026-09-22 | PR #18 re-review | GATE PASS
Greptile re-scanned after the prior fix commit and flagged one more P1: the DEC-169 fix (unknown routing member -> temporary delivery error) was itself flawed — a temporary error still counts as a real attempt against `retry.Coordinator`'s `DefaultAttemptLimit` (5), so an unknown-locally member would exhaust and PERMANENTLY fail the message after 5 attempts instead of holding indefinitely for a restart, exactly the failure mode the v0.39 kill switch exists to prevent. Fixed at the correct layer: added `worker.MemberRegistryGate` (`MemberKnownLocally`), checked by `holdIfMemberDisabled` BEFORE any SMTP work — mirrors the disabled-member hold exactly, spends zero retry attempts. `routing.Router` gained `Known(memberID) bool`; `databaseOutcomeStore` now carries a `*routing.Router` reference and implements the new gate by delegating to it, wired at `cmd/mailx/serve.go`'s `databaseOutcomeStore{..., router: router}`. `Router.Deliver`'s error-returning path (DEC-169) stays as defense-in-depth only. Added 4 new `internal/worker` tests (none existed for the disabled-member hold path before this review, a gap from the original v0.39 work) using a frozen clock so the hold path is deterministic, not sleep-based: unknown-locally holds with zero attempts, disabled-member holds with zero attempts, known+enabled proceeds normally, and an unreadable routing-enabled state (DB error) also degrades to a hold. Plus a `Router.Known` unit test. `go build`/`vet`/`gofmt`: clean. `go test -count=1 ./...` and `-race ./...`: all packages pass (real Postgres + Redis). Docker Compose rebuild + boot smoke: clean. Ilana updated: architecture.md, decisions.md (DEC-174), state.json.

## 2026-09-22 | v0.41 | GATE PASS
Confirmed v0.40 complete before starting: PR #18 merged into main (8a90259), synced local main, branched feranmi/v0.41-batch-sending. No new migration — batch items become ordinary messages/outbox rows via the existing InsertMessage transaction, no "batch" concept below the API layer. Implementation is a careful refactor, not new logic: extracted handleSend's entire per-message acceptance sequence (template render, outbound.Build, authorizeAndSign, checkRecipientsForAcceptance, idempotency claim, admitSend, FileStore.Save, InsertMessage) into a shared acceptOne(ctx, tenantID, req, idemKey, now, scheduledAt) method; changed authorizeAndSign/checkRecipientsForAcceptance to return *apiError instead of writing directly to http.ResponseWriter (required so a batch caller can attribute a failure to one item without corrupting the shared response). Ran the full pre-existing email_handler_test.go/idempotency_test.go suite (109 tests) immediately after the refactor, before writing any new batch code, to prove zero behavioral regression — all passed unchanged. New POST /v1/emails/batch: up to 100 items (MaxBatchSize, matching existing maxLimit=100/defaultMaxRecipients=50 conventions), fully independent items (own transaction, own admitSend call — a batch cannot buy more throughput than N individual sends, proven by TestBatchSendAbuseControlsAppliedPerItemIndependently with TenantMaxQueuedMessages=2), partial-failure response shape ({data:[{index,email?,error?}],accepted,rejected}), per-item optional idempotency_key (no batch-level header — reuses the exact same Fingerprint/ClaimIdempotencyKey/replay machinery). Oversized/empty batch or any malformed per-item idempotency key rejects the WHOLE request before any item is processed (verified by asserting zero messages exist afterward). 12 new tests in internal/api/email_batch_test.go. gofmt/go vet/go build: clean. go test -count=1 ./... and -race ./...: all packages pass (real Postgres + Redis). OpenAPI updated (new /emails/batch path, BatchSendRequest/BatchSendItem/BatchSendResponse/BatchSendResultItem/BatchItemError schemas) and drift-tested (TestOpenAPIRoutesMatchRuntime passes). Docker Compose rebuild + boot smoke: clean. Ilana updated: milestones.md, architecture.md (new "Batch Sending" section), decisions.md (DEC-175..178), state.json. Committed locally on feranmi/v0.41-batch-sending, not pushed.

## 2026-09-22 | PR #19 review | GATE PASS
Both Strix and Greptile flagged findings on PR #19 (v0.41 Batch Sending). Both valid, both fixed: (1) Strix LOW/CWE-400 — requestLimitMiddleware charges Cost:1 per HTTP request regardless of body size, so a 100-item batch bought up to 100x the DKIM-signing/DB work of a normal send for one rate-limit token. Fixed by charging the SAME tenant/API-key request buckets the remaining N-1 tokens a batch this size actually costs, in handleSendBatch itself, BEFORE any per-item work runs — a refusal rejects the whole batch (429) with zero items touched (DEC-179). (2) Greptile P1 — pollIdempotencyCompletion can block up to 5s per item on an in-progress key; sequential across up to 100 batch items this could exceed writeTimeout (15s) and have the connection close mid-response, silently dropping unprocessed items despite the endpoint's one-result-per-item contract. Fixed with a batch-wide processing deadline: emailHandler.batchBudget (a field defaulted to 10s in newEmailHandler, mirroring the existing `now` field pattern so tests can inject a near-zero budget deterministically) bounds the whole per-item loop; once spent, remaining items get an explicit batch_processing_timeout result instead of being silently dropped (DEC-180). Added 4 new tests: proportional rate-limit charge causing wholesale batch refusal, single-item batch charges nothing extra, deadline-exhausted items get explicit timeout results with zero silently dropped (verified via an artificially near-zero batchBudget, not a real multi-second wait), plus the existing 10 batch tests all still pass unchanged. gofmt/go vet/go build: clean. go test -count=1 ./... and -race ./...: all packages pass (real Postgres + Redis). Docker Compose rebuild + boot smoke: clean. Ilana updated: architecture.md, decisions.md (DEC-179/180), milestones.md, state.json.

## 2026-09-23 | v0.42 | GATE PASS
Confirmed v0.41 complete before starting (PR #19 merged into main, local Feranmi_works fast-forwarded to match). Source-truth check found the existing `runSMTPReceiver` (port 2525) is an unauthenticated local dev-receive sink (writes straight to FileStore, no accept pipeline) — confirmed distinct from what v0.42 needed, left untouched. Found `internal/smtp` already has a real inbound server package (`server.go`/`listener.go`) with no AUTH/TLS support — extended it rather than building a new one: added `Config.RequireAuth`/`Authenticator`, an `AUTH` command case (PLAIN mechanism, inline base64), and a MAIL-FROM auth gate. Chose implicit TLS (SMTPS-style, connection is TLS from the start) over STARTTLS mid-connection upgrade — avoids restructuring the existing buffered-reader connection state machine, and matches RFC 8314's own recommendation (DEC-181). New `api.SubmissionAcceptor` wraps the existing unexported `emailHandler.acceptOne` so submitted mail reuses the exact REST acceptance pipeline (DKIM, domain auth, suppression, abuse controls, durable InsertMessage) rather than a second implementation (DEC-183). Added 5 focused tests in `internal/smtp/auth_test.go` (auth required before MAIL, AUTH PLAIN success/failure, malformed base64, auth-disabled path unaffected) using the package's existing `net.Pipe()` harness. Before committing, ran a REAL end-to-end smoke test outside the unit suite: built the binary, started it locally against the disposable Docker Postgres/Redis, created a tenant/API-key/verified-domain, generated a self-signed cert, and sent a real message via Python's `smtplib.SMTP_SSL` with AUTH PLAIN — caught a real bug this way that no unit test would have: the disabled-by-default submission receiver returned `nil` immediately instead of blocking on `ctx.Done()`, which `runComponents` treats as a crashed component and uses to cancel every other component, taking the whole server down on every normal boot (submission is off by default). Fixed (DEC-184), reverified via Docker Compose boot logs (submission component now starts and stays up cleanly). Test data and local artifacts (cert, keys, tenant, test binary) cleaned up after verification; found and flushed accumulated stale Redis test-suite keys from prior sessions as an unrelated pre-existing disposable-dev-infra cleanup. `gofmt`/`go vet`/`go build`: clean. `go test -count=1 ./...` and `-race ./...`: all packages pass (real Postgres + Redis). Docker Compose rebuild + boot smoke: clean, submission component healthy. No schema/migration. Ilana updated: milestones.md, architecture.md (new "SMTP Submission" section), decisions.md (DEC-181..184), state.json. Committed on Feranmi_works (per updated instruction: all work on this branch going forward), not pushed.

## 2026-09-23 | v0.43 | GATE PASS
Confirmed v0.42 complete before starting. Read v0.38's existing open/click deferral reasoning (DEC-155) first so this milestone's defaults didn't contradict it: opt-in per send (track_opens/track_clicks), off by default, no template-level toggle (out of scope). Reuses the existing events table (two new types, opened/clicked) rather than new aggregate infrastructure, matching v0.38's posture. Chose HMAC-signed self-contained tokens over a new tracked-links table: no DB writes at send time for link rewriting, and the click redirect is open-redirect-proof by construction (MailX signs the exact destination itself, never trusts a client-supplied URL). internal/tracking (new, tiny) does Sign/Verify. Injection (internal/api/tracking_inject.go) runs in acceptOne after template rendering, before outbound.Build/DKIM signing, so tracked HTML is what actually gets signed. Links containing "unsubscribe" are never rewritten (source-truth check confirmed MailX has no unsubscribe-link feature yet to special-case further). New public routes GET /track/open/{token} (1x1 GIF) and GET /track/click/{token} (302), active only when MAILX_TRACKING_SECRET/MAILX_TRACKING_BASE_URL are configured — unconfigured silently no-ops rather than producing broken links. Deliberately did NOT wire opened/clicked into internal/webhook's separate subscription-filter vocabulary (out of scope; confirmed the fanout path tolerates unmapped types harmlessly, so this is a safe explicit deferral). 11 new tests (token sign/verify/tamper/malformed, injection no-op/rewrite/pixel/unsubscribe-exclusion, end-to-end send+click+redirect+event, send+open+pixel+event, invalid-token 404) plus zero regressions in the existing suite. gofmt/go vet/go build: clean. go test -count=1 ./... and -race ./...: all packages pass (real Postgres + Redis). Migration 000024 validated fresh-from-zero AND via a manual up/down/up round-trip against the disposable dev DB. Docker Compose rebuild + boot smoke: clean, tracking correctly inert with no secret configured. Ilana updated: milestones.md, architecture.md (new "Open & Link Tracking" section), decisions.md (DEC-185..189), state.json. Committed on Feranmi_works, not pushed (PR to be opened after the next milestone per explicit instruction).

## 2026-09-24 | v0.44 | GATE PASS
Confirmed v0.43 complete before starting. Read the current OpenAPI spec's full path list (34 endpoints across 14 resource groups) as source of truth for coverage, rather than trusting earlier milestones' own descriptions of their endpoints. Built sdk/node (TypeScript) and sdk/python, both outside the Go module directory — zero build/test coupling confirmed via `git status` showing no changes outside sdk/ before commit. Both SDKs share the same design: one generic request method (auth header, JSON body, retry on 429/5xx honoring Retry-After with exponential backoff+jitter, capped at 3 retries by default) plus one typed method per OpenAPI operation. Core send/batch/get/list-email methods are fully typed (hand-mirrored from internal/api's SendEmailRequest/Email/BatchSendRequest/BatchSendResponse, not code-generated); every other resource gets a typed signature with a loosely-typed JSON body/result, proportionate to the task's token budget rather than building a JSON-Schema-to-TS/Python codegen pipeline. React Email integration needed no SDK code at all once traced through: @react-email/render's output is already the exact html/text shape sendEmail accepts, so it's a two-line README pattern, not a bundled dependency. Node SDK: 3 unit tests (request shape + auth header, 429-retry-then-succeed honoring Retry-After, MailXError mapping on 422) all pass; TypeScript compiles clean (tsc, strict mode). Python SDK: 4 unit tests (send success, 429 retry, error mapping, missing-api-key validation) all pass using a mocked requests.Session (no responses library available in this environment). Both SDKs also have a contract test gated on MAILX_SDK_TEST_BASE_URL/_API_KEY that runs a real send against a live server when configured; verified BOTH skip cleanly (not fail) when unset, but did not actually run them against a live server this session since Docker was unavailable (paused) — noted explicitly as a deferred verification, not silently skipped. Ilana updated: milestones.md, architecture.md (new "Official SDKs" section), decisions.md (DEC-190/191), state.json. Committed on Feranmi_works (Go side untouched, confirmed before commit), not pushed — PR to be opened per the user's standing instruction after the next milestone.

## 2026-09-24 | v0.44 amendment | Go SDK added
User asked for a Go SDK to match Node/Python, plus a recommendation for further languages. Added sdk/go as a third, separate Go module (github.com/Ferousco-dev/mailx-go, go 1.21) with zero build/test coupling to the main server module — confirmed via git status outside sdk/go/ being empty before commit. Same design as Node/Python: generic retrying request core (429/5xx retried up to WithMaxRetries, default 3, honoring Retry-After else backoff+jitter) plus typed methods per OpenAPI resource; core email methods (SendEmail/SendBatch/GetEmail/ListEmails) fully typed against internal/api's field names, remaining 10 resource groups typed-signature/loose-JSON-body via mailx.JSON. Idiomatic Go shape: functional options, context.Context first param, *APIError on failure. 5 unit tests (httptest.Server: success, 429-retry-then-succeed, non-retryable 4xx error mapping, get-by-id) all pass; go build/go vet/gofmt all clean; gated live contract test (contract_test.go) verified to skip cleanly without MAILX_SDK_TEST_BASE_URL/_API_KEY, not run live (Docker still unavailable this session, same deferral as v0.44's original Node/Python note). Ilana updated: milestones.md (v0.44 entry amended, not duplicated), architecture.md ("Official SDKs" section extended), decisions.md (DEC-192), state.json (DEC counter). Committed on Feranmi_works, not yet pushed — PR still outstanding per the user's standing "open a PR after the next milestone" instruction.

## 2026-09-24 | v0.44 amendment | PHP + Ruby SDKs and release automation
User asked for a release workflow (GitHub Release on merge to main, notes marked "still under development") and for PHP and Ruby SDKs to round out Node/Python/Go. Added .github/workflows/sdk-release.yml: on push to main, tars each sdk/<lang> directory and publishes a GitHub Release tagged sdk-YYYY.MM.DD-<sha> via softprops/action-gh-release, with the body explicitly stating the SDKs are under active development, APIs may change, and none are on a package registry yet — a snapshot artifact, not a versioned release. Added sdk/php (Composer package mailx/sdk, PHP >=8.1, PSR-4 autoload) with a Transport interface (CurlTransport default, swappable for tests) and sdk/ruby (gem mailx-sdk, Ruby >=2.6, stdlib Net::HTTP with an injectable http_client callable for tests) — same design as the other three SDKs: generic retrying request core (429/5xx retried up to a configurable max default 3, honoring Retry-After else backoff+jitter) plus one method per OpenAPI resource. Unlike Node/Python/Go, PHP/Ruby use plain array/hash in-and-out for every method including email send, not typed request/response structs — a deliberate language-fit choice, not a scope cut. Hit one PHP-specific bug: declaring a typed $code property on MailXException collided with the parent \Exception::$code (PHP fatal error, caught by php -l), fixed by renaming to $apiCode. PHP: 4 unit tests pass (PHPUnit, FakeTransport), contract test skips cleanly. Ruby: 4 unit tests pass (Minitest, lambda-based fake http_client), contract test skips cleanly. Neither run against a live server this session (Docker still unavailable, same deferral as the first three SDKs). Confirmed via git status that the Go server side and the three existing SDKs were untouched before commit. Ilana updated: milestones.md (v0.44 entry amended again), architecture.md (Official SDKs section extended with PHP/Ruby specifics and release automation), decisions.md (DEC-193, DEC-194), state.json (DEC counter). Committed on Feranmi_works, not yet pushed — five SDK commits plus this one now sit ahead of the last push; PR still outstanding per the user's standing instruction.

## 2026-09-24 | v0.45 | GATE PASS
Compared v0.45 (Deliverability Insights) vs v0.46 (Data Retention/GDPR) for the user first: v0.45 is read-only over existing durable events with no design prerequisite, v0.46 needs an explicit retention-policy decision before any code (per its own milestone note) — recommended doing v0.45 first, user agreed. Confirmed v0.38's analytics layer already existed (overview/timeseries/broadcast endpoints) before starting, so this was an extension, not a new subsystem: read internal/database/analytics.go and internal/api/analytics_handler.go first rather than assuming from the roadmap description. Added opened/clicked (v0.43's existing event types) to AnalyticsCounts; added a server-computed rates object to the overview response (ratio against queued, the only mutually-exclusive-safe denominator per the package's own documented semantics); added GET /v1/analytics/domains grouping CURRENT recipients.status by recipient domain (joined through messages for tenant scope), capped at 50 domains. Zero new tables, zero new writes, zero new event types, zero migrations. Caught one real bug during testing: domain extraction produced "example.com>" because real /v1/emails sends store recipient addresses in raw "<addr>" header form (unlike hand-built test fixtures using bare addresses) — the DB-level test with bare addresses passed while the handler-level test using a real send caught it; fixed with a trim(trailing '>') in the SQL. Docker Desktop was manually paused again this session; unpaused it via `osascript activate` + `docker desktop start`, then `docker compose up -d` succeeded — full test suite (go test ./... and go test -race ./...) ran clean against real Postgres+Redis, zero regressions, 4 new DB tests + 5 new API handler tests all passing. gofmt/go vet/go build: clean. Ilana updated: milestones.md (v0.45 flipped to COMPLETE), architecture.md (new "Deliverability Insights" section), decisions.md (DEC-195), state.json (last_completed_milestone, next_milestone, DEC counter). User then asked to push and open a PR bundling all commits currently ahead of origin (v0.42 SMTP submission through this v0.45 work, plus the Node/Python/Go/PHP/Ruby SDKs and the SDK release workflow).

## 2026-09-24 | OpenAPI/Swagger standardization | GATE PASS
User feedback: "the current [swagger docs] looks some how" - asked for standard docs a company can use. Diagnosed via grep: zero `tags`, zero `operationId` anywhere in the 1324-line hand-written spec, so Swagger UI rendered all 34 operations as one flat alphabetical list with no resource grouping, plus a bare `info` block (title/version/description only, no contact/license) and a single relative server URL. Rather than hand-editing 34 operations individually, wrote a one-off Python script (json.load/json.dumps, order-preserving) to add tags (13 resource groups: Emails, Domains, DKIM, SPF, DMARC, BIMI, Templates, Contacts, Audiences, Broadcasts, Analytics, Suppressions, Webhooks, each with a description) + operationId to every operation, enrich info with contact/license, and add a production server entry alongside the existing self-hosted relative one. This reformatted the file from hand-compacted JSON to one-key-per-line indent=2 (1324 -> ~4770 lines) - a deliberate, disclosed tradeoff toward readability/diffability over compactness, matching how most public OpenAPI specs are actually shipped. Also upgraded the `/docs` Swagger UI page: deepLinking, persistAuthorization, a tag/operation filter box, alpha-sorted tags, hidden topbar. Verified zero behavioral change: TestOpenAPIRoutesMatchRuntime/TestOpenAPISpecParses/TestServedOpenAPIDocumentsRateLimitContract all pass against real Postgres+Redis, gofmt/go vet/go build clean, full suite (go test ./...) clean. Rebuilt the Docker image (`docker compose up -d --build mailx`) and visually verified in the browser pane: tags render as collapsible groups with descriptions, filter box works, server selector shows both URLs, contact/license links present. Ilana updated: architecture.md (OpenAPI section amended), decisions.md (DEC-196), state.json (DEC counter). This is a presentation-only change to an existing surface (v0.18's OpenAPI system), not a new milestone - no milestones.md/state.json milestone-pointer changes.

## 2026-09-24 | PR #20 Greptile review fixes | GATE PASS
User asked to check the PR and pull Greptile's dropped comments. Fetched via gh api repos/.../pulls/20/comments: 6 findings, 4 P1 + 2 P2. Verified each against the actual codebase rather than trusting the comment text at face value (per source-truth discipline) - all 6 held up. Fixed all 6: (1) SMTP submission sink was building sends from parsed headers instead of the session's real MAIL FROM/RCPT TO envelope, silently dropping Bcc and substituting the envelope sender - extracted a testable submissionEnvelope() function and added 2 unit tests. (2) click-tracking regex only matched double-quoted href, silently leaving single-quoted links untracked - widened the regex, added a regression test. (3) Go/PHP/Ruby SDKs parsed error bodies from the response root instead of the real nested {"error":{...}} envelope (confirmed Node/Python already did this correctly, so only 3 of 5 SDKs needed the fix) - fixed all three, fixed their unit tests' fixture bodies to match, all still pass. (4) same 3 SDKs called nonexistent routes: ListEvents hit /v1/emails/{id}/events instead of the real GET /v1/events, SetBIMI used PUT on a route that's GET+POST/verify only, SendBroadcast called a send endpoint that doesn't exist (broadcasts send automatically on creation) - fixed routes, renamed SetBIMI to GetBIMI+VerifyBIMI, removed SendBroadcast entirely from all 3 SDKs. (5) new DomainBreakdown query (v0.45) filtered on recipients.created_at which has no index - switched to messages.created_at (recipients are inserted in the same transaction) to reuse the existing idx_messages_tenant_created index instead of adding a new migration; added an EXPLAIN-plan regression test at realistic (0.25%) selectivity confirming an Index Scan, following the exact pattern of the pre-existing TestAnalyticsHotQueriesUseIndexes - first version of this test was itself wrong (100% selectivity, so seq scan was actually correct) and had to be corrected to properly backdate most rows outside the query window before it caught anything meaningful. (6) sdk-release.yml ran two actions via mutable tags with contents:write - pinned both to commit SHAs (resolved via gh api against the actual latest tags, not guessed versions) and moved the write permission from workflow-level to job-level. Verified everything: gofmt/go vet clean, go build clean, full go test ./... clean against real Postgres+Redis (Docker unpaused again this session), PHP/Ruby unit tests re-run and passing, no stale references to removed/renamed SDK methods in any README. Ilana updated: decisions.md (DEC-197), state.json (DEC counter). Pushed to Feranmi_works, updating PR #20 in place.

## 2026-09-24 | PR #20 follow-up Greptile comment | GATE PASS
User asked to check for and fix the latest Greptile comment. A new P1 landed after the previous fix push: "Bcc recipients become visible" - my DEC-197 fix for the envelope-discarded bug had put the full RCPT TO list into the request's `to` field, which meant `outbound.Build` (which derives visible To/Cc headers directly from those fields, deliberately excluding only the `Bcc` field from headers) would now leak every Bcc address into the visible outgoing To header - trading a delivery bug for a privacy bug. Verified against outbound/builder.go directly rather than trusting the comment: confirmed Bcc is excluded from headers only when passed via the dedicated Bcc field, and there's no separate envelope-override mechanism in Request. Fixed by having submissionEnvelope derive to/cc from the parsed message headers (preserving visible structure) and inferring bcc as whichever envelope RCPT TO addresses are NOT in those headers (the correct signal, since real MUAs never transmit a Bcc header line to their MSA at all). Rewrote both unit tests for the new 4-return-value signature. Full suite re-run clean. Ilana updated: decisions.md (DEC-198), state.json (DEC counter). Pushed to Feranmi_works, updating PR #20 again.
