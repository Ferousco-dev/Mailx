# Decisions

- DEC-001: Rigour 3 because MailX is production-oriented email infrastructure.
- DEC-002: Potential harm includes privacy exposure, false delivery diagnosis, credential leakage, and false health signals.
- DEC-003: Treat addresses, message content, API keys, webhook secrets, and infrastructure credentials as sensitive.
- DEC-004: Use a hybrid process: gated requirements/security verification with incremental Go construction.
- DEC-005: No fixed deadline is assumed; correctness and privacy outrank speed.
- DEC-006: The user is owner and approver; automated regression evidence compensates for a single-person implementation context.
- DEC-007: Success requires end-to-end correlation, low-cardinality metrics, safe health signals, privacy tests, and all validation gates passing.

## Recovered architectural decisions (2026-09-21 historical recovery; source: Git + code, see `milestones.md` / `architecture.md`)

Process decisions above (DEC-001..DEC-007) belong to the v0.23 FLEET run and stay current for v0.23.

- DEC-008 [v0.1-v0.5]: SMTP is implemented at protocol level (raw TCP, own state machine and reply handling) rather than via a library; the project's stated purpose is to understand and own the protocol. Evidence: `09cec8f` -> `internal/smtp`, README.
- DEC-009 [v0.3]: `mail.Envelope` (MAIL FROM/RCPT TO) is separate from `mail.Message` (headers/body) because delivery uses envelope recipients; headers never control routing (hidden Bcc). Evidence: `internal/mail/model.go`.
- DEC-010 [v0.5]: MailX assigns its own message ID; the `Message-ID` header is untrusted metadata. Raw `.eml` is stored byte-exact plus derived metadata; attachments are metadata-only on load. Evidence: `internal/storage/store.go`.
- DEC-011 [v0.6]: Transport limits are finite by default (zero value selects a finite default, never "unlimited" for size). Evidence: `internal/smtp/config.go`.
- DEC-012 [v0.7]: `Accepted` means 2xx to the DATA terminator; later QUIT failure is recorded, never reverses acceptance (RFC 5321 4.1.1.10). Evidence: `smtp/client.go`, `transfer.go`, `delivery.go`.
- DEC-013 [v0.9]: DNS package only discovers; Null MX (RFC 7505) is terminal and never falls back to implicit MX; NXDOMAIN and temporary failure are distinct; ASCII-only domains until IDNA is designed. Evidence: `internal/dns/resolver.go`.
- DEC-014 [v0.10]: MX fallback only for pre-session failures; a remote answer after session engagement is authoritative to avoid duplicate work/odd DSNs. Evidence: `delivery/policy.go`.
- DEC-015 [v0.11]: Retry defaults fail safe (zero Decision = TerminalFailure); Coordinator performs exactly one operation and never waits; caller owns timing and State. Evidence: `retry/`.
- DEC-016 [v0.12]: DSNs claim only what MailX can truthfully know (omit unknown RFC 3464 fields; unconfirmed sibling recipients get a generic diagnostic); generation is separate from sending. Evidence: `bounce/`.
- DEC-017 [v0.13/v0.16]: Queue is at-least-once by design; nothing can make remote SMTP and Ack atomic. Redis holds scheduling and claim ownership only. Lua scripts make each transition atomic; polling chosen over blocking primitives. Evidence: `queue/queue.go`, `redis.go`.
- DEC-018 [v0.14]: Fixed-size worker pool, no goroutine-per-job; worker depends on narrow interfaces (Loader, Coordinator, OutcomeStore). Evidence: `worker/worker.go`.
- DEC-019 [v0.15]: PostgreSQL is durable truth; tenant_id on every table from day one; crypto-random TEXT IDs; raw MIME stays in FileStore; per-operation `delivery_attempts` with MX detail as JSONB; indexes justified by real queries + EXPLAIN tests. Evidence: migration 000001.
- DEC-020 [v0.17]: Deterministic schema setup via a one-shot `migrate` service; Redis AOF; loopback-only ports. Evidence: `compose.yaml`.
- DEC-021 [v0.18]: Transactional outbox closes the commit-then-crash-before-enqueue gap; HTTP 202 = durably accepted. Dispatcher relies on duplicate-safe Enqueue plus guarded `MarkOutboxDispatched`, no new lock primitive. Evidence: migration 000004, `dispatch/`.
- DEC-022 [v0.18]: stdlib `net/http` (no framework); public API schemas decoupled from DB/worker structs; hand-written OpenAPI with drift test; mixed-domain sends rejected before acceptance because the engine delivers per domain. Evidence: `internal/api`.
- DEC-023 [v0.19]: API key = `mx_<key_id>_<secret>`, HMAC-SHA256 verifier (+pepper), no password hashing (256-bit random secret); rotation via `expires_at` grace; identical generic 401; scopes enforced in Go and DB CHECK; no public key-creation endpoint; rows never deleted. Evidence: `internal/auth`, migration 000006.
- DEC-024 [v0.20]: Idempotency arbiter is PostgreSQL only; scoped to tenant; fingerprint over validated request; completion in the same tx as the message insert; stale-claim reclaim with fingerprint-guarded completion; replay reflects current state. Evidence: `idempotency`, migration 000007, `8ba5264`..`389c31d`.
- DEC-025 [v0.21]: Domain ownership by DNS TXT proof with strict normalization; pending claims may overlap across tenants, verified ownership is unique; verification does not imply SPF/DKIM/DMARC. Evidence: `internal/domain`, migration 000009.
- DEC-026 [57ee1d0]: Delivery outcome (attempt + status + event) persists in one transaction before queue finalization; worker consults durable state before SMTP; queue `Renew` protects live workers during DB outages; retry state rebuilt from PostgreSQL. Replaces best-effort status reporting. Evidence: `worker/processor.go`, `database/outcomes.go`, migration 000010.
- DEC-027 [v0.22]: `events` is the single event truth; webhooks are derived fan-out (separate tables, atomic fan-out, per-subscription delivery rows). Public event names are a stable mapping decoupled from internal names; `delivery_attempted` stays internal. Evidence: `database/events.go`, `webhook_fanout.go`.
- DEC-028 [v0.22]: Webhooks are at-least-once with stable event ID; consumers deduplicate; no exactly-once claim. Evidence: OpenAPI text, `UNIQUE(subscription_id,event_id)`.
- DEC-029 [v0.22]: Signing secrets need recoverable storage, so AES-256-GCM with a master key and tenant-ID AAD instead of one-way hashing; HMAC-SHA256 `v1` signature over `timestamp.body`. Evidence: `webhook/secret.go`, `signing.go`.
- DEC-030 [v0.22]: SSRF defence is enforced at dial time as well as at creation to defeat DNS rebinding; no proxies, no redirects. Evidence: `webhook/policy.go`, `client.go`.
- DEC-031 [v0.22]: Webhook delivery uses PostgreSQL leases with fencing tokens and `SKIP LOCKED`, not Redis, so notification work is independent of the email queue. Evidence: `database/webhook_deliveries.go`.
- DEC-032 [recovery]: `.ilana/` had never been committed and stopped being maintained after the initial milestones; from 2026-09-21 Ilana updates are mandatory per task (`CLAUDE.md`).
- DEC-033 [v0.23]: Observability is a leaf package; components get optional logger/metrics (nil-safe, panic-guarded) so behavior is unchanged when absent. Prometheus official client with a private pedantic registry; label values allowlisted else `other`.
- DEC-034 [v0.23]: Operator listener is separate from the developer API (`MAILX_OBSERVABILITY_ADDR`, default `:9090`, empty disables); developer `/health/ready` shares the bounded readiness checker and no longer echoes raw errors.
- DEC-035 [v0.23]: Queue depth and ping are concrete `RedisQueue` methods (interface unchanged); depth and its error counter come from one collector so a scrape is self-consistent.
- DEC-036 [v0.23]: A worker survives transient queue-claim failures (DEF-004) so dependency outages degrade readiness instead of killing the process.
- DEC-037 [v0.24]: Outbound STARTTLS is a small explicit state machine in `client_tls.go`; `raw` vs `conn` split keeps deadlines race-free across the upgrade; capabilities are nil'd when the handshake starts and only ever set from the latest EHLO.
- DEC-038 [v0.24]: Two policies only (`opportunistic` default, `required`). Default is opportunistic because RFC 3207 forbids public servers from requiring STARTTLS; it never downgrades after attempting TLS (stricter than MTAs that retry plaintext after a failed handshake).
- DEC-039 [v0.24]: Certificate verification is always on (no insecure option). Opportunistic therefore verifies too; peers with self-signed/mismatched certs are deferred until fixed or trusted via `MAILX_SMTP_TLS_CA_FILE` (extra roots added to the system pool). An unverified-encryption mode was deliberately not built.
- DEC-040 [v0.24]: All TLS-stage failures are temporary (statements about the connection, not the recipient) and the delivery engine tries the next MX for them (before MAIL FROM, same policy), reusing the existing retry model.
- DEC-041 [v0.24]: Inbound STARTTLS is deferred (needs certificate provisioning/reload); the inbound EHLO must not advertise it.
- DEC-042 [v0.24]: Test infrastructure lives in `internal/smtp/smtptest` (ephemeral in-memory CA + scriptable fake MX) so smtp, delivery-style and worker pipeline tests share one deterministic TLS server.
- DEC-043 [v0.25]: Trusted relay is an explicit `delivery.Config.Relay`; relay mode replaces MX routing entirely and never falls back to direct delivery, so an operator's "deliver through this relay" intent cannot be silently bypassed.
- DEC-044 [v0.25]: Credentials exist only inside `Config.Relay` and per-request `DeliveryRequest.Auth`; the SMTP client holds none. This makes "credentials never reach a DNS-discovered MX" structural rather than a runtime check.
- DEC-045 [v0.25]: Mechanisms PLAIN (preferred) and LOGIN only, only over verified TLS; credentials force TLS-required regardless of policy. OAuth/XOAUTH2, SCRAM, CRAM-MD5 deferred or rejected (see design doc).
- DEC-046 [v0.25]: Every auth-stage failure is a temporary attempt at the engine (existing backoff, never another MX): authentication describes relay configuration, not the message, so bouncing senders permanently for an operator error was rejected.
- DEC-047 [v0.25]: Remote AUTH reply text is discarded (code and enhanced status only) and error text is a fixed category, because servers may echo credentials.
- DEC-048 [v0.26]: rsa-sha256 with 2048-bit keys only, relaxed/relaxed; Ed25519 deferred (uneven receiver support, would need dual signing). Own canonicalization, standard-library crypto, verified by an independent implementation in tests.
- DEC-049 [v0.26]: sign at API acceptance, so stored = queued = transmitted bytes and signing failures reject the request instead of failing after acceptance; retries resend the stored (already signed) bytes.
- DEC-050 [v0.26]: a domain with an active key ALWAYS signs and any key failure refuses the send (no unsigned fallback); a domain without an active key sends unsigned (DKIM not set up). Pending keys never sign.
- DEC-051 [v0.26]: verified-From is an exact canonical-name match against the tenant's verified domains (v0.21 root/subdomain independence preserved), enforced before durable acceptance and re-checked in the insert transaction.
- DEC-052 [v0.26]: private keys are AES-GCM encrypted with a dedicated `MAILX_DKIM_MASTER_KEY` that must differ from the webhook key; `internal/secretbox` extracted so both use one reviewed primitive; ciphertext bound to tenant/domain/selector via AAD; retired keys lose their ciphertext; domain deletion deletes keys.
- DEC-053 [v0.26]: lifecycle is pending -> active -> retired with partial unique indexes and a per-domain row lock for concurrency safety; rotation = create + publish + verify, and the old key signs until activation.
- DEC-054 [v0.27]: sending is NOT blocked when SPF is missing, wrong or unverifiable. Authorization = verified domain ownership, signing = active DKIM key, SPF = readiness information; a stricter "require full authentication" policy is a later, explicit production decision. Local dev must not need public DNS.
- DEC-055 [v0.27]: SPF identity is the MAIL FROM domain (= From-address domain in MailX). HELO is `mailx.local` and not SPF-capable; changing it is v0.29 deliverability work, only documented here.
- DEC-056 [v0.27]: sending infrastructure is operator-declared (`MAILX_SENDING_IPS` public-only, or `MAILX_SPF_RELAY_INCLUDE`), mutually exclusive with the routing mode; no egress auto-discovery; in relay mode MailX invents no authorization for infrastructure it does not control.
- DEC-057 [v0.27]: verify only the literal subset (ip4/ip6 or named include); never follow include/a/mx/exists/ptr/redirect (unbounded DNS work, RFC 7208 4.6.4 limit); say so via `unevaluated_mechanisms` rather than claim full evaluation. One DNS query per verification.
- DEC-058 [v0.27]: exactly one SPF record per domain: existing records are extended (mechanisms inserted at the front so they match first), multiple records are a `conflict` with no generated value, oversized merges are refused.
- DEC-059 [v0.27]: SPF state is not persisted (DNS is truth; no stale copies); GET gives guidance without DNS, POST verify does one lookup, requires a verified owned domain (no DNS probing of arbitrary names) and stores nothing. Distinct statuses replace any single verified boolean so v0.28 DMARC can use them.
- DEC-060 [v0.28]: RFC 9989 is the DMARC standard used (RFC 7489 is obsolete); differences from 7489 are followed and documented.
- DEC-061 [v0.28, SUPERSEDED by DEC-066]: organizational domain for alignment = ICANN PSL registrable domain through `domain.Normalize` (offline, deterministic), not the RFC 9989 tree walk; never suffix matching. Policy discovery walks only from the domain to its organizational domain (max 8 queries).
- DEC-062 [v0.28]: recommended initial policy is `p=none` with no rua/ruf; existing policies are never rewritten or duplicated; enforcement progression is the owner's decision (MailX has no report intake to prove alignment holds).
- DEC-063 [v0.28]: readiness is sender-side only and separate from any receiver result (`receiver_result: not_observed`); distinct dns/path/readiness states replace a single boolean; relay-mode SPF alignment is `unknown`, never faked.
- DEC-064 [v0.28]: missing or invalid DMARC never blocks sending (authorization = ownership, signing = DKIM, SPF/DMARC = readiness); DMARC cannot grant From authorization.
- DEC-065 [v0.28]: no persistence for DMARC (DNS is truth); DKIM/SPF are consumed as facts through interfaces so `dmarc` imports neither and cannot alter them.
- DEC-066 [v0.28 correction]: the RFC 9989 DNS Tree Walk (sections 4.10, 4.10.1, 4.10.2) is the sole authority for the DMARC Organizational Domain and for policy discovery, as ONE walk so both come from the same interpretation; `psd=n`/`psd=y` are implemented; the PSL and suffix matching are forbidden in `internal/dmarc` production code (tested). Supersedes DEC-061.
- DEC-067 [v0.28 correction]: multiple DMARC records at a name are discarded and the walk continues (RFC 9989 4.10 step 2), but MailX reports `conflict` when that hides the domain's own record or leaves no governing policy, because receivers silently fall back and the owner must fix it.
- DEC-068 [v0.28 correction]: any DNS error during the walk fails safe (`temporary_error`, readiness `unknown`); no parent policy is substituted and no boundary guessed; relaxed alignment that could not obtain a boundary is `unknown`, never "not aligned" or "ready". Strict and identical-domain alignment never need DNS.
- DEC-069 [deployment, 2026-09-21, user decision]: the first production/deployment target is an AWS Lightsail Linux instance on the $7/month plan (fixed static IP included). Sources for the plan: user choice; AWS promo of 3 free months on select $5/$7/$12 plans and student credits (AWS Free Tier, AWS Educate) were reported by web search, not verified against AWS. The $7 bundle is 'Micro-1GB Linux with public IPv4': 2 vCPU, 1 GB RAM, 40 GB SSD, 2 TB transfer, per the user's own Lightsail console listing (not independently verified by us). The $5 'Micro-1GB Linux IPv6-only' plan has the same hardware but no public IPv4 and is unsuitable for MailX (mail reputation, SPF/PTR, port 25 request and tooling all depend on IPv4). Measured stack memory on the dev machine: MailX ~8.5 MiB, PostgreSQL ~248 MiB, Redis ~7 MiB (~265 MiB). Whether PostgreSQL stays on the same host or moves to a managed service (Aiven free plan: 1 CPU, 1 GB RAM, 1 GB storage, max_connections 20, no static IP, single node) is decided in DEC-070.
- DEC-070 [deployment, 2026-09-21, user decision]: on the AWS Lightsail $7 host (DEC-069) run MailX, PostgreSQL and Redis together on the one server (docker compose). Reasons: measured stack ~265 MiB fits a 1 GB host after tuning PostgreSQL memory and adding a small swap file; one thing to operate; no network hop per database call; no dependency on a free managed plan that could change; hosted TLS/password Redis is unsupported anyway (architecture limitation 19). Backups are mandatory from day one: a nightly `pg_dump` copied off the server, because API keys, domains and encrypted DKIM keys live only in PostgreSQL (RSK-025; the raw MIME store on local disk is a separate backup concern). Managed PostgreSQL (for example Aiven) is a LATER option if the box is outgrown or managed backups are wanted; it is a `DATABASE_URL` change (`sslmode=require`), and latency from Lightsail must be measured before switching. Not yet implemented: no deployment, tuning or backup script exists in the repository.
- DEC-071 [v0.29]: the SMTP infrastructure hostname (`MAILX_SMTP_HOSTNAME`) is an operator concept separate from tenant domains: it is configured, never derived from a tenant domain, and does not align with any tenant's DMARC domain; tenants never own PTR.
- DEC-072 [v0.29]: identity modes are static and explicit: sending IPs declared => hostname required (startup fails); hostname present => strict validation; neither => local `mailx.local`; relay without hostname => local with a warning. DNS is never consulted at startup or in health.
- DEC-073 [v0.29]: generated Message-IDs use the infrastructure hostname (RFC 5322 3.6.4), built once before DKIM signing; no user-supplied Message-ID exists to preserve; the signed stored bytes are never mutated.
- DEC-074 [v0.29]: readiness is per-IP FCrDNS with membership semantics; multiple PTR names pass with a warning only when ours is among them; any definite failure makes the set not ready, uncertainty makes it unknown, and a healthy IP never hides a broken one; nothing is persisted.
- DEC-075 [v0.29]: infrastructure diagnostics are an operator CLI, not an API or health check (PTR is the operator's; caller-supplied lookups would be an SSRF-style probe; health must not depend on public DNS).
- DEC-076 [v0.29]: HELO SPF is advisory guidance (RFC 7208 2.3 recommends verifiers check it; it is the only identity for null-reverse-path mail) and never gates `ready`; PTR is never SPF authorization.
- DEC-077 [deployment, 2026-09-21, user decision]: PostgreSQL memory tuning ships as an OPT-IN Compose override (`compose.low-memory.yaml`, documented in `docs/low-memory-deployment.md`), NOT as the global defaults. Evidence (measured here on `postgres:16`, same `pgbench -i -s 5` + `-c 5 -j 2 -T 8` load on fresh instances): 122.2 MiB with stock settings vs 50.8 MiB tuned (`shared_buffers=32MB`, `max_connections=20`, `work_mem=2MB`, `maintenance_work_mem=16MB`, `wal_buffers=1MB`, `max_wal_size=256MB`, `effective_cache_size=128MB`, fewer autovacuum/background/parallel workers). The long-running dev database shows ~288 MiB from accumulated data and cache; Redis ~7 MiB and MailX ~8 MiB are already small. Reasoning: the default must keep local development and the test suite on stock settings; the profile targets ~1 GB hosts (the Lightsail $7 plan, DEC-069/070). Durability is preserved and pinned explicitly (`fsync`, `synchronous_commit`, `full_page_writes` on) because MailX persists outcomes before acknowledging jobs and accepts an email only after commit. 20 connections suffice (pool 10 + 3 reserved + migrate/CLI); the whole suite passed six times against a server with `max_connections=20`.
- DEC-078 [v0.30]: suppression is tenant-scoped policy (`UNIQUE (tenant_id, email)`), separate from delivery history; creating or deleting one never rewrites, retries or resurrects anything; cross-tenant access is 404.
- DEC-079 [v0.30]: ONE canonical key function (`suppression.Normalize`) for API, worker and store; ASCII only; the local part is lower-cased for the KEY ONLY (fail-safe direction for a deny list; RFC 5321 2.4 discourages case-sensitive local parts; transport still preserves case); dots and `+tag` are never folded; every accepted recipient must be keyable.
- DEC-080 [v0.30]: reasons manual|hard_bounce|complaint|unsubscribe and sources api|delivery|feedback are stored vocabulary (WHY vs HOW) but only manual/api and hard_bounce/delivery are produced; the API creates only `manual`, so callers cannot forge system-derived facts; complaint/unsubscribe/feedback wait for v0.32 and a subscription product.
- DEC-081 [v0.30]: auto-suppress only a recipient-specific permanent mailbox rejection: permanent RCPT TO with 5.1.1 or 5.1.6 (RFC 3463); never temporary failures, sender/policy/authentication/content failures, domain-level codes, mailbox-full/disabled, or a bare 550. Suppression commits in the same transaction as the outcome.
- DEC-082 [v0.30]: enforce suppression at BOTH acceptance (fast feedback; all suppressed => 422 nothing stored, key not consumed; partial accepted) and delivery (authoritative, per attempt including retries, before any transport; recipient-level: suppressed recipients are removed from the envelope, all suppressed => zero SMTP).
- DEC-083 [v0.30]: consistency boundary: the worker's check is the guarantee; a suppression created afterwards does not revoke an in-flight attempt; no DB lock across SMTP I/O; accepted delivery history is immutable.
- DEC-084 [v0.30]: fail safe: unknown suppression state defers the job (15 s, no attempt, no counter movement) instead of sending or failing the message.
- DEC-085 [v0.30]: new terminal message status and recipient status `suppressed` (not failed/bounced) and one lifecycle event `suppressed`/`email.suppressed` (counts only, once per message); list mutations are not events.
- DEC-086 [v0.30]: unsuppression is a hard delete (simple uniqueness and a trivial safety-critical query; cost: no removal audit trail); no Redis cache, PostgreSQL only, measured ~0.36 ms per attempt.
- DEC-087 [v0.31]: rate limiting is GCRA (a token bucket kept as one timestamp) in a single atomic Redis Lua script with Redis `TIME` as the only clock: O(1) state, exact Retry-After, multi-bucket all-or-nothing so a refused request charges nothing, one round trip per decision; rejected: fixed windows (burst at boundaries), sliding-window logs (memory per request), per-process limiters (wrong with several processes).
- DEC-088 [v0.31]: the tenant is the primary boundary and API keys can never raise a limit: the request tenant bucket is charged by every key, a per-key guard (<= tenant limit) only stops one key using the whole allowance.
- DEC-089 [v0.31]: charge order and idempotency: replays and 409s are free; after the claim is owned check tenant queue cap, then global backlog, THEN charge recipient tokens (least side effect first); recipient cost = deliverable (unsuppressed) recipients; a refusal deletes the claim it owns so the same key retries after Retry-After. Trade-off: tokens are not refunded if PostgreSQL fails after the charge.
- DEC-090 [v0.31]: failure policy: sends and other mutating requests FAIL CLOSED when the limiter is unavailable (503 + Retry-After) because an outage must not mean unlimited sending (auth/readiness precedent); GETs fail open so a Redis blip is not a full outage; unknown permit state defers delivery without an attempt. Rejected: fail open everywhere (unmetered abuse during outages).
- DEC-091 [v0.31]: 429 means the tenant/key exceeded ITS limit; 503 means MailX is at capacity or cannot enforce limits; both carry Retry-After (whole seconds, exact for rate limits, fixed 30 s for capacity); OpenAPI adds them programmatically to every operation so the contract cannot drift.
- DEC-092 [v0.31]: fair dispatch by round robin across tenants (recursive loose index scan + LATERAL per-tenant slice + rank interleave) instead of global `available_at` order; drop `FOR UPDATE SKIP LOCKED` (no-op in an implicit transaction); migration 000014 replaces `idx_outbox_pending` with `idx_outbox_pending_tenant` because with both, the planner filtered 15,000 rows behind a large backlog (EXPLAIN evidence) and one index halves outbox write cost.
- DEC-093 [v0.31]: per-tenant and per-destination concurrency are lease permits (ZSET holder->expiry, prune on acquire, TTL crash recovery), taken after suppression and before any transport; refusal defers with jitter and records no attempt, so backpressure can never fail or bounce a message; release uses a fresh context.
- DEC-094 [v0.31]: retry storms are broken with deterministic bounded jitter (FNV of the scheduling instant, +/- N%, never above Max, never below 1 s), default 10%, configurable 0-50; it is not randomness a test cannot pin.
- DEC-095 [v0.31]: safety configuration is strict: an invalid value fails startup naming the variable (contrast lenient tuning knobs), cross-field rules are validated, `off` is the only opt-out and is logged loudly; abuse metrics use closed label sets (max 30 series) and never carry tenant/key/address/domain.
- DEC-096 [v0.32]: feedback lands at the RECIPIENT level (`recipients.feedback_status/feedback_at`), never rewriting `delivery_attempts`/`messages.status`; a multi-recipient message's coarse status is not collapsed to one recipient's later feedback.
- DEC-097 [v0.32]: correlation token = HMAC-SHA256(secret, messageID), truncated 128 bits, hex; ingestion route also accepts a bare message_id because the route itself requires an operator-only credential — VERP Return-Path (`bounce+<token>@bounce-domain`) was evaluated and deferred to avoid a MAIL FROM change and new DNS this milestone (RSK-036).
- DEC-098 [v0.32]: `EventBounced` (reserved since v0.18) is reused for feedback-driven bounces rather than adding a second bounce event type; `EventComplained`/`email.complained` is new.
- DEC-099 [v0.32]: idempotency key = UNIQUE(message_id, recipient_id, sha256(raw bytes)); the recipient-level state transition (not just the history row) additionally gates suppression/event emission, so a semantically-repeated but byte-different DSN never double-fires.
- DEC-100 [v0.32]: async suppression reuses the exact narrow enhanced-status set as v0.30's synchronous rule (5.1.1/5.1.6), factored into `suppression.hardBounceEnhancedStatuses`, so the two paths cannot diverge.
- DEC-101 [v0.33]: template rendering is a hand-written bounded `{{identifier}}` substitution, not text/template — avoids exposing an action language (if/range/pipelines) as an unintended attack surface; deterministic, single-pass, no recursive expansion.
- DEC-102 [v0.33]: HTML variable substitution is always `html.EscapeString`-ed; no raw/unescaped-HTML variable mode in v0.33 (conservative default, explicitly the only model).
- DEC-103 [v0.33]: template send is mutually exclusive with explicit subject/html/text (422 on conflict) — never a silent precedence choice.
- DEC-104 [v0.33]: idempotency fingerprint is computed on the ORIGINAL request (template_id+variables), not rendered output, so a template edit between an accepted send and a same-key retry cannot create a duplicate or change the replayed content — it replays the original durable message.
- DEC-105 [v0.33]: template name uniqueness is a DB constraint (`UNIQUE(tenant_id,name)`), not only application validation — same precedent as domain-name uniqueness.
- DEC-106 [v0.33]: template deletion is a hard delete with no versioning; already-sent messages are independent rows/files, unaffected by template edits or deletion.
- DEC-107 [v0.34]: contact identity normalization is a SEPARATE function from suppression.Normalize — domain folding shared, but local-part case is PRESERVED (not folded), because a contact identifies a real mailbox where merging case variants risks conflating two people, unlike a deny-list where over-blocking is the safe direction.
- DEC-108 [v0.34]: contacts and suppressions have zero FK/cascade relationship in either direction; contact deletion/recreation/update never touches suppression state, by construction (verified: separate tables, separate normalization, no trigger).
- DEC-109 [v0.34]: contact attributes are a bounded flat string map (JSONB storage, CHECK-enforced object shape + size), not arbitrary JSON — avoids nested/array/executable payloads without inventing a schema system.
- DEC-110 [v0.34]: sending never requires or auto-creates a contact; Contacts is additive, not a gate on `/v1/emails`.
- DEC-111 [v0.34]: no `contact.*` webhook events and no contact-specific metrics in v0.34 — neither has an identified consumer yet; recorded as a deliberate omission, not an oversight.
- DEC-112 [v0.35]: audience membership tenant integrity is enforced by composite FKs on `audience_members` (tenant_id + audience_id, tenant_id + contact_id), not application checks alone — a cross-tenant row is a schema-level impossibility, verified by mutation-removing the app-level check and confirming the isolation test still fails correctly.
- DEC-113 [v0.35]: `audience_members` cascades on EITHER side (audience delete or contact delete) removes ONLY join rows; contacts/audiences/suppressions are never cascade-deleted into each other.
- DEC-114 [v0.35]: adding an already-present member is idempotent (204, no error), matching suppression's create-if-absent precedent rather than 409 — a group-membership add is naturally repeatable, unlike a named-resource create.
- DEC-115 [v0.35]: membership pagination keys on the membership row's own created_at, not the contact's, for stable ordering independent of contact history.
- DEC-116 [v0.35]: no sending, no snapshot, no `audience.*` webhook events in v0.35 — scope held strictly to group membership per the milestone boundary.
- DEC-117 [v0.36]: a broadcast recipient is materialized as one independent normal `messages` row via the EXISTING InsertMessage path — never one SMTP transaction with many RCPT TO commands; RSK-029 (multi-recipient RCPT all-or-error) is therefore structurally avoided by this design, not fixed.
- DEC-118 [v0.36]: audience/template/personalization are all SNAPSHOTTED (audience via a timestamp watermark + copied rows, template source copied verbatim, contact attributes copied at snapshot time) so a broadcast's eventual content is fully determined at acceptance and never depends on later mutation of the source Audience/Template/Contact.
- DEC-119 [v0.36]: `broadcasts.audience_snapshot_at` is stamped by PostgreSQL's own `now()`, not a Go timestamp — found during testing that Go/DB clock skew inside the dev container could otherwise silently exclude the entire audience.
- DEC-120 [v0.36]: expansion is two bounded phases (snapshot, materialize) driven by one poller mirroring `internal/dispatch`'s established pattern, in per-tick batches (200/25), never a single unbounded transaction or an in-memory full-audience load.
- DEC-121 [v0.36]: `broadcast_recipients.id` doubles as the eventual `messages.id`; materialization is idempotent by checking message existence before InsertMessage, so quota is never double-charged on crash-retry.
- DEC-122 [v0.36]: suppression is checked again immediately before each recipient's materialization (not only cached from acceptance), so a suppression created mid-broadcast still protects not-yet-processed recipients.
- DEC-123 [v0.36]: broadcast DELETE and PATCH are NOT implemented in v0.36 — deliberately deferred rather than inventing ambiguous "delete means cancel" semantics; an accepted broadcast's configuration is immutable.
- DEC-124 [v0.36]: the pending-recipient partial index was corrected from `(broadcast_id)` to `(broadcast_id, created_at)` after EXPLAIN evidence at 50k rows showed the planner otherwise preferring a full-scan-and-filter plan (~115x more buffer reads) once most recipients were already processed.
- DEC-125 [v0.37]: Broadcast scheduling extends the EXISTING v0.36 expander via `ClaimActiveBroadcasts`'s runnable criteria (`send_at IS NULL OR send_at <= now()`) rather than building a second scheduler/poller. Normal-email scheduling reuses the pre-existing `messages.outbox.available_at` mechanism (present since v0.18, untouched).
- DEC-126 [v0.37]: `send_at` is validated as a strict RFC 3339 absolute instant, rejected if in the past, shared between POST /v1/emails (`scheduled_at`) and POST /v1/broadcasts (`send_at`) via one `parseSendAt` helper (`internal/api/scheduling.go`). No natural-language or bare-time parsing.
- DEC-127 [v0.37]: due-work comparisons for BOTH the outbox and Broadcasts are PostgreSQL-clock-authoritative (`available_at <= now()` / `send_at <= now()`, computed inside SQL), not Go-clock. The outbox's `ListPendingOutbox`/`fairOutboxSQL` previously took a Go `now time.Time` parameter; removed in this milestone, applying the same clock-skew lesson v0.36 already learned the hard way for its acceptance watermark.
- DEC-128 [v0.37]: scheduled Broadcast snapshot semantics are ACCEPTANCE-time, unchanged from v0.36 — `audience_snapshot_at` is stamped by `CreateBroadcast` regardless of `send_at`. Scheduling therefore never widens RSK-038: a member added to the Audience after acceptance is excluded whether the broadcast sends immediately or days later. Proven by `TestScheduledBroadcastSnapshotIsAtAcceptanceNotActivation`.
- DEC-129 [v0.37]: suppression and abuse controls are NOT frozen at Broadcast/email scheduling time; both remain evaluated at their existing late boundaries (materialization for Broadcasts, dispatch for normal email) — scheduling introduces no bypass.
- DEC-130 [v0.37]: no reschedule/cancel endpoint. `send_at` is immutable after acceptance (same immutability posture as v0.36's broadcast configuration, DEC-123); attempting a different `send_at` under the same Idempotency-Key is a 409 conflict, not a silent reschedule (fingerprint already hashes the whole validated request).
- DEC-131 [v0.37]: no new scheduler state/table. `broadcasts.send_at` (nullable `TIMESTAMPTZ`, migration 000020) is the entire durable representation; a future-dated broadcast is simply invisible to `ClaimActiveBroadcasts` until due — no separate "scheduled" status.
- DEC-132 [v0.37]: template content, contact attributes, and domain/DKIM authorization are NOT re-validated at Broadcast activation beyond what v0.36 already re-validates (template/contact are frozen snapshots from acceptance; domain authorization is checked once at acceptance, matching pre-existing v0.36 behavior — no new revalidation was added or removed).
- DEC-133 [v0.37]: `idx_broadcasts_active_due (send_at, created_at) WHERE status IN ('accepted','expanding')` supports the due-scan; EXPLAIN at 2003 active broadcasts (2000 future, 3 due) shows a bitmap index scan, 8 buffer hits, 0.018ms — no sequential scan (`TestClaimActiveBroadcastsDueScanAtScale`, MET-072).
- DEC-134 [PR review, v0.32-v0.37 PR]: `broadcast_recipients` claiming (materialization) is now an atomic lease (`claimed_at`, migration 000021) instead of a bare `SELECT ... FOR UPDATE SKIP LOCKED`. The plain SKIP LOCKED query's row lock released as soon as the query returned — before Go-side render/sign/quota-charge/InsertMessage ran — so two truly-concurrent expander instances could both claim and process the same recipient (Greptile P1). `ClaimActiveBroadcasts`/`SnapshotBroadcastBatch` were audited and found already safe (the latter holds a real per-broadcast transaction+lock; the former's downstream calls are individually guarded), so only the recipient-materialization path needed the fix.
- DEC-135 [PR review]: added a terminal `broadcast_recipients.status = 'failed'` after `maxRecipientAttempts` (5) consecutive materialization failures, instead of retrying (and recharging tenant quota for) a deterministically-failing recipient forever, which could also block its broadcast from ever completing (Greptile P1).
- DEC-136 [PR review]: `mergeVariables` now applies the recipient's OWN name/email unconditionally (name only when non-empty, email always) AFTER globals/attributes, not merely when the key was absent — a global template variable named "name" or "email" was previously overriding every recipient's personalization (Greptile P1, broke the documented recipient-wins contract from v0.36).
- DEC-137 [PR review, 2nd round]: `materializeOne` failures are split into deterministic (render/MIME-build/parse — wrapped in `errDeterministic`, eligible for the bounded terminal-failure counter added in DEC-135) vs transient (DKIM lookup, filesystem, DB — never wrapped, always retried indefinitely). The first version of DEC-135 counted every error uniformly, which meant 5 unlucky transient failures (e.g. a momentary DKIM key store hiccup) could permanently drop a recipient that a 6th tick would have delivered (Greptile P1, caught on the fix commit itself).
- DEC-138 [PR review, 3rd round]: `classifyDKIMError` splits `dkim.SignError` by `Outcome`: `OutcomeKeyUnavailable` (the key-store DB lookup itself failing) stays transient/always-retried; every other outcome (corrupt/undecryptable key, public-key mismatch, domain mismatch, hard sign failure) is deterministic given the domain's STORED key state and is eligible for the bounded terminal-failure counter (DEC-135/137). Previously every `SignMessage` error was treated as transient, so a permanently broken key could block a broadcast from ever completing (Greptile P1).
- DEC-139 [PR review, 3rd round]: added `storage.ErrRecordExists` and `materializeOne` now treats it as idempotent success (skip re-save, proceed to InsertMessage) rather than a failure. Previously, if `FileStore.Save` succeeded but `InsertMessage` then failed/crashed, every retry hit "record already exists" and was (wrongly) treated as a normal failure — which recurs identically forever, permanently blocking that recipient (Greptile P1, same class as DEC-134/135 but at the filesystem layer).
- DEC-143 [ad-hoc, user-directed 2026-09-22, SUPERSEDES DEC-140..142]: reverted the `priority: "urgent"` opt-in tier (DEC-140..142) after user feedback that requiring callers to remember to flag time-critical mail is bad ergonomics. Replaced `DefaultBackoffPolicy`'s single exponential schedule (30min/1h/2h/4h/4h) with one universal front-loaded schedule (5s/5min/30min/2h/5h) applied to EVERY send automatically — inspired by the pattern in Resend's documented webhook-retry schedule (front-loaded, slow later), not a literal copy of it (Resend's published schedule is for webhook callbacks, not SMTP delivery retries — no separate SMTP-retry schedule was available to compare against). No field, no flag, no per-tenant config: transient failures on ANY send now get discovered in seconds instead of 30 minutes, while a genuinely down destination still gets backed off to hours (same overall ~7.5h exhaustion window, spent differently). `BackoffPolicy` gained a `Schedule []time.Duration` field (checked before the older Base/Max exponential path) to represent this irregular front-loaded sequence, which does not fit a fixed multiplicative ratio.
- DEC-144 [PR review]: `DefaultBackoffPolicy`'s schedule is exactly 4 entries (5s/5min/30min/2h), not 5 — with `DefaultAttemptLimit` at 5 operations, only 4 delays are ever scheduled (the delay after the LAST allowed attempt is never used, since exhaustion follows immediately). The prior 5-entry version's trailing 5h entry was dead code, and the documented ~7h35m total exhaustion was wrong — real exhaustion is ~2h35m (Greptile P1, caught on `d421615`). Accepted as the new deliberate total: faster overall abandonment of a persistently-failing destination is an acceptable tradeoff of front-loading, not something requiring a 5th entry to compensate for.
- DEC-145 [PR review]: the DNS-timeout workaround (DEC-143's note) moved out of `compose.yaml`'s default `mailx` service into an opt-in overlay (`compose.dns-workaround.yaml`, layered with `-f`). Hardcoding public resolvers unconditionally would break any developer on a VPN or a network with split-horizon/internal DNS, and doesn't even bypass the actually-flaky component (Compose queries still transit the embedded DNS proxy either way) — Greptile P2, caught on `3b5672b`.
- DEC-146 [PR review, 4th round]: `storage.FileStore.Save` now self-heals a crash-partial message directory (created but missing message.eml/metadata.json from a prior crash) instead of returning `ErrRecordExists` for it — a caller trusting that error as "prior success" (the documented idempotent-retry contract) could otherwise durably reference a record the worker can never load, permanently unsendable (Greptile P1). Repair only fires once the directory is older than `repairGracePeriod` (5s): completeness-checking alone is not a safe trigger, since two genuinely concurrent Save calls for the same id both see the directory mid-write — repairing on that basis corrupted the write and broke `TestFileStoreSaveConcurrentSameID`'s "exactly one concurrent Save wins" invariant during this fix's own development (caught immediately by the existing test, confirmed via 10x mutation reruns: 8/10 failed without the age guard, 10/10 pass with it).
- DEC-147 [PR review, 4th round, CONSIDERED AND REVERTED]: attempted the same atomic-lease fix already applied to `ClaimPendingBroadcastRecipients` (DEC-134) on `ClaimActiveBroadcasts` too (Greptile P1, "broadcast claims release early"). Reverted after it broke `TestExpanderBoundedAcrossManyTicksForLargerAudience`: unlike a recipient (claimed once, processed within one tick), a broadcast is LEGITIMATELY re-claimed by the SAME expander instance every tick across potentially many ticks while its audience is still snapshotting/materializing — a multi-minute lease blocks the owning instance from ever seeing its own broadcast again until the lease expires, breaking normal single-instance progress. Re-confirmed the original RSK-040 reasoning instead: `ClaimActiveBroadcasts`'s SKIP LOCKED lock releasing early is real, but every downstream operation it feeds (`SnapshotBroadcastBatch`'s own per-broadcast transaction+lock; the now-atomically-leased `ClaimPendingBroadcastRecipients`; `MarkBroadcastExpanding`/`TryCompleteBroadcast`'s status-guarded idempotent updates) is ALREADY individually safe against concurrent double-processing — the residual risk is wasted redundant work under genuine multi-instance concurrent overlap, never a duplicate send or duplicate quota charge. Accepted as RSK-040's documented residual, not fixed with a lease; a correct fix (if ever needed) would require a heartbeat-renewed lease tied to the owning instance's tick loop, not a fixed-duration lease — deferred as unnecessary complexity for a risk with no correctness impact.
- DEC-148 [v0.38]: analytics is a pure read layer over the EXISTING `events` table — no new aggregate/derived table, no ClickHouse/Kafka/warehouse. `events` already records exactly the lifecycle facts needed (one row per transition, historically timestamped, tenant-scoped, already indexed via `idx_events_tenant_occurred`), so a live indexed query is sufficient at measured scale (50k rows, EXPLAIN evidence: index scan, 17 buffer hits, <0.15ms for a selective 1-day window). Revisit only if real production scale proves this insufficient.
- DEC-149 [v0.38]: metric facts are NOT mutually exclusive. A message's earlier `queued`/`delivered` fact is never erased or reclassified by a later asynchronous `bounced`/`complained` fact — both are counted, preserving real history (see `TestAnalyticsOverviewCountsByEventTypeWithinRange`). `email.bounced` is the SAME internal event type for both a synchronous rejection and v0.32's async DSN feedback (unchanged from v0.32's own design).
- DEC-150 [v0.38]: `CurrentlySuppressed` (overview only) is a CURRENT-STATE snapshot (`count(*) FROM suppressions`), deliberately NOT folded into the time-bucketed `AnalyticsCounts` — it answers "how many addresses are suppressed right now", independent of the requested `[from,to)` range, unlike every other field which is a historical fact count within that range.
- DEC-151 [v0.38]: time range contract is `[from,to)` half-open RFC 3339, capped at `AnalyticsMaxRange` (90 days) at the API layer; timeseries additionally caps bucket COUNT at `AnalyticsMaxBuckets` (744), rejected analytically from the range/interval BEFORE running the query (not just after seeing the result), so a huge range with no events yet (e.g. a future range) still gets rejected — checking only the returned row count would miss that case.
- DEC-152 [v0.38]: timeseries buckets are SPARSE — an interval with zero events is simply absent from the response, never zero-filled server-side. Chosen over zero-filling because MailX has no way to know a client's preferred display granularity beyond the request itself, and zero-filling risks quietly producing a much larger response than the bucket-count cap intends to bound.
- DEC-153 [v0.38]: Broadcast analytics reads recipient/orchestration counts from `broadcast_recipients` (the durable snapshot frozen at acceptance, per v0.36) — NEVER live Audience membership, which may have changed since. Proven with a dedicated test that removes an Audience member after the snapshot and confirms `Intended` is unaffected (mutation-caught: joining against `audience_members` instead breaks the test).
- DEC-154 [v0.38]: new `analytics:read` scope (migration 000022), not implied by `emails:read`/`broadcasts:read`/any existing scope — a key scoped to read one message must not automatically see tenant-wide aggregates. No `analytics:write`: analytics has no writable resource of its own.
- DEC-155 [v0.38]: open/click tracking (pixels, link rewriting, redirect infrastructure) deliberately NOT implemented this milestone — major privacy/security/deliverability/semantic issues (Apple Mail Privacy Protection, image proxies, security scanners, and bots all produce false "opens"/"clicks"), and no prior product decision in source truth called for it. The API never returns an `open_rate`/`click_rate` field at all (omitted, not a misleading zero) — see the package doc in `internal/database/analytics.go`.
- DEC-156 [v0.38]: no caching layer (Redis or otherwise) — first measure PostgreSQL (DEC-148's EXPLAIN evidence), which is sufficient. Consistency model: near-current at query time, directly from PostgreSQL truth — not eventually consistent, since there is no async aggregate processor to lag behind.
- DEC-157 [v0.39]: three-layer routing model, kept deliberately separate: configured POLICY (`sending_pools`/`sending_pool_members`, operator-mutable anytime) vs the durable per-message ROUTING DECISION (`messages.sending_member_id`, set once at `InsertMessage` acceptance, retry-sticky, never re-selected) vs the per-attempt historical TRANSPORT SNAPSHOT (`delivery_attempts.{sending_member_id,transport_kind,effective_hostname,effective_source_ip}`, denormalized with no FK, so it keeps answering "what actually sent this" even after the member/pool config is later changed or the member is disabled). `messages` intentionally has NO `sending_pool_id` column — the pool is only a selection-time input; once a member is chosen the pool is no longer operationally relevant, and storing both would create two competing "current routing" pointers.
- DEC-158 [v0.39]: pools/members are operator-global infrastructure — no tenant ownership, no tenant-writable fields, CLI-only management (`sendingpools.go`, mirroring `create-tenant`/`create-api-key`'s CLI-only pattern). `domains.sending_pool_id` is set exclusively via `AssignDomainPool`/`assign-domain-pool`, never through the tenant-facing domain create/update API — a tenant must not gain the ability to pick arbitrary operator sending infrastructure merely because the column exists. No pool/member deletion in v0.39, disable (`enabled=false`) only, so a historical `sending_member_id` reference can never go dangling.
- DEC-159 [v0.39, user-directed correction of the initial proposal]: `enabled=false` (pool OR member) is a real kill switch, not advisory — but it NEVER reroutes or reassigns. `worker.holdIfMemberDisabled` runs before any SMTP work for a message whose durable member (or that member's pool) is currently disabled: zero SMTP attempts, zero attempt records, `messages.sending_member_id` untouched, job released via the existing claim/outbox mechanism with a fixed `memberDisabledHold` (30s) bound — never 0, never unbounded, so a disabled member cannot cause a hot retry loop. Resumes through the SAME member automatically once re-enabled. This is deliberate: v0.39 has NO failover/rotation logic at all, which is what keeps a disabled member from being an evasion path around receiver policy/reputation.
- DEC-160 [v0.39, user-directed correction of the initial proposal]: `delivery_attempts` gets its own historical transport snapshot columns (`transport_kind`/`effective_hostname`/`effective_source_ip`) rather than relying solely on `sending_member_id` for "what infrastructure actually sent this" — `sending_pool_members.hostname`/`source_ip` are mutable and the id alone would go stale (or, once deletion is ever supported, dangling) the moment an operator edits a member. Populated by `routing.Router.Deliver` snapshotting `delivery.Result.Effective{Hostname,SourceIP,MemberID}` (set only on the actual dispatch path, never the legacy fallback) at attempt time; `transport_kind` reuses the pre-existing `delivery.Result.Transport` ("direct"/"relay", already set by every `Engine`), so it is populated for every attempt, pooled or not. No secrets/credentials are ever part of this snapshot.
- DEC-161 [v0.39, PR-review-class fix found by this package's own migration-upgrade regression tests]: `InsertMessage`'s and `PersistDeliveryOutcome`'s SQL only reference the migration-000023 columns (`sending_member_id`/`transport_kind`/`effective_hostname`/`effective_source_ip`) when at least one actually has a non-nil/non-empty value; the legacy (no pool configured) case uses the byte-for-byte pre-v0.39 INSERT statement. Required because `internal/database`'s existing migration-upgrade tests (`TestWebhookMigrationUpgradesDurabilityPrerequisite`, `TestDeliveryOutcomeMigrationUpgradesV021Data`, `TestSuppressionMigrationUpgradesAndDowngradesExistingData`) roll a test schema back to the previous migration(s) and then insert using CURRENT Go code before upgrading — a standing regression check for the rolling-deploy window where new app code briefly runs against an older schema. Also fixed a real `pgx` bug this suite caught: a PostgreSQL `inet` column cannot scan directly into a Go `*string` (binary-format mismatch) — every `source_ip`/`effective_source_ip` read must wrap the column in `host(...)` first.
- DEC-162 [v0.40]: BIMI is entirely derived/live, like `internal/dmarc` and `internal/spf` before it — no new table, no migration. DNS (and, only when asset validation is explicitly requested, the fetched logo/certificate) is the sole source of truth; nothing is persisted. Revisit only if a real MailX-owned mutable BIMI resource emerges (none does in v0.40: pools/members/domains already carry everything needed).
- DEC-163 [v0.40]: BIMI's DMARC prerequisite is "effective policy != none", full stop — no separate `pct=` check. Some BIMI guidance (written against the older RFC 7489) states "if `p=quarantine`, `pct=100` is mandatory"; MailX's own DMARC (`internal/dmarc`, following RFC 9989 which obsoletes 7489) already discards/ignores `pct` entirely as a removed tag (see DMARC's own `WarnDeprecatedTag` handling), so every DMARC state MailX can report already represents full enforcement whenever policy isn't none. Recorded as a deliberate conflict resolution (task instruction: prefer the conservative interpretation) rather than re-deriving `pct` semantics BIMI-side, which would have meant a second, competing DMARC evaluator — exactly what was explicitly disallowed.
- DEC-164 [v0.40]: only the domain-wide `default` selector is checked. The BIMI draft also defines a per-message `BIMI-Selector` header mechanism for publishers running several concurrent logos; MailX adds no BIMI headers to outbound mail and has no per-message selector concept, so there is nothing for a custom selector to select between — deferred, not silently ignored (see architecture.md).
- DEC-165 [v0.40]: SVG validation (`internal/bimi.ValidateSVG`) checks the SECURITY-RELEVANT structural subset of the SVG Tiny 1.2 Portable/Secure profile MailX can verify safely and truthfully — version/baseProfile attributes, a square viewBox, a required title, and the absence of script/foreignObject/image/style/animate elements and any external href — using Go's `encoding/xml` (which never resolves external entities/DTDs) with explicit depth (64) and node-count (20000) bounds. This is NOT a certification of full SVG Tiny 1.2 P/S conformance; a mailbox provider's own renderer remains the real authority there. Matches the task's "validate only what MailX can truthfully validate" instruction.
- DEC-166 [v0.40]: certificate (VMC/CMC) handling is structural-only — `crypto/x509.ParseCertificate` plus a NotBefore/NotAfter window check, reporting Subject/Issuer/CurrentlyValid as facts. No chain building, no revocation check (OCSP/CRL), no comparison against a BIMI-authority trust anchor ("Minimum Security Requirements for Issuance of Mark Certificates" is a separate, unimplemented specification), no private CA, no private-key storage. `Parseable=true` must never be read as "a mailbox provider would accept this certificate."
- DEC-167 [v0.40]: logo/certificate fetching reuses `internal/webhook.URLPolicy`/its DNS-rebinding-safe `DialContext` (re-resolves and re-validates at actual dial time, not just at URL-parse time) rather than building a second SSRF-hardening implementation. `internal/bimi.AssetFetcher` adds BIMI-specific bounds on top: HTTPS-only (no `AllowHTTP`/`AllowPrivate` override, unlike webhooks — a BIMI asset is always meant to be a real public resource), a hard size cap via `io.LimitReader(maxSize+1)`, and — since the BIMI draft does not specify redirect handling for `l=`/`a=` — the conservative choice of following NO redirects at all (`http.ErrUseLastResponse`), matching MailX's existing webhook-client precedent for the same `CheckRedirect` pattern.
- DEC-168 [v0.40]: every `bimi.Result` (and its JSON `BimiStatus` resource) carries a fixed, non-optional `Disclaimer` field stating that MailX's readiness reflects its own DNS/DMARC/asset checks only and is never a guarantee of mailbox-provider display — enforced structurally (the field is required in the OpenAPI schema, not just documented in prose), directly satisfying the task's hard requirement that MailX must never claim "BIMI valid = logo will appear."
- DEC-169 [PR #18 review, Greptile P1]: `routing.Router.Deliver` no longer falls back to `Base` for a `MemberID` missing from the process-local registry (stale after a member was added, or a direct member skipped for a missing hostname, since last restart). Silently using `Base` could dial with the wrong source IP/EHLO identity or bypass a relay without any record of it — exactly the kind of silent misrouting v0.39 was designed to prevent. It now returns a `*delivery.Error{Kind: KindTransferTemporary, Temporary: true}` wrapping the new `routing.ErrUnknownMember`, so the existing retry/backoff machinery holds the message (same "never silently reroute" posture as `worker.holdIfMemberDisabled`, DEC-159) until an operator restarts the process to refresh the registry.
- DEC-170 [PR #18 review, Greptile P1]: `buildRouter` (cmd/mailx/serve.go) now takes `relayConfigured bool` and skips mapping a `relay`-kind pool member to `base` unless a relay is actually configured (`MAILX_RELAY_*` set). Previously a relay member on a process running in pure direct mode would silently perform direct MX delivery, contradicting the operator's declared kind and recording `transport_kind=direct` for what was configured as a relay member. Omitted relay members become "unknown member" at dispatch (DEC-169's hold/retry path), not a silent wrong-transport send.
- DEC-171 [PR #18 review, Greptile P1]: BIMI readiness (`internal/bimi/readiness.go`) now checks `CertInfo.CurrentlyValid` in addition to `Parseable` before reporting `ready` when a certificate was fetched and validated — a structurally parseable but expired (or not-yet-valid) certificate now correctly reports `certificate_issue`, not `ready`. `CurrentlyValid` was already computed by `ParseCertificate` and exposed through the API; the readiness combinator simply wasn't consulting it.
- DEC-172 [PR #18 review, Greptile P1]: `bimi.Service.lookupOne`'s DNS error handling now distinguishes a definitive "no record" (`*net.DNSError` with `IsNotFound && !IsTemporary && !IsTimeout`) from a transient resolver failure (SERVFAIL, timeout), matching the exact pattern `internal/spf` and `internal/dmarc` already use. Previously every non-context-deadline lookup error was read as `not_configured`, which could (a) misreport a transient outage as "no BIMI record" and (b) incorrectly treat that as non-decisive and trigger the organizational-domain fallback, producing a definitive-looking result from what was actually an unresolved DNS failure.
- DEC-173 [PR #18 review, Greptile P2]: `buildBIMI`'s shared `AssetFetcher` is now bounded to `MaxCertificateBytes` (64KB, the larger of the two v0.40 asset caps) instead of `MaxLogoBytes` (32KB) — a certificate between those two sizes was previously rejected as `asset_too_large` before `ParseCertificate` ever saw it, even though the certificate validator itself accepts up to 64KB. The tighter logo bound is still enforced correctly: `ValidateSVG` applies its own `MaxLogoBytes` check to whatever bytes the fetcher returns, so an oversized SVG is still rejected, just by the validator rather than the fetch layer.
- DEC-174 [PR #18 re-review, Greptile P1, found on DEC-169's own fix]: DEC-169's fix (unknown member -> temporary delivery error) was itself flawed — a temporary error routed through the normal `retry.Coordinator` DOES consume a real attempt slot, so after `DefaultAttemptLimit` (5) attempts the message is permanently exhausted and failed, exactly the outcome the disabled-member kill switch was designed to avoid. Fixed properly: added `worker.MemberRegistryGate` (`MemberKnownLocally(memberID string) bool`), checked by `holdIfMemberDisabled` BEFORE `MemberRoutingGate`/before any SMTP work — an unknown-locally member now holds via the existing bounded `memberDisabledHold` release, consuming zero retry attempts, exactly like a disabled member. `routing.Router` gained a `Known(memberID) bool` method; `databaseOutcomeStore` now holds a `*routing.Router` reference and implements `MemberKnownLocally` by delegating to it. `Router.Deliver`'s own error-returning unknown-member path (DEC-169) remains as defense-in-depth for any caller that doesn't have the registry gate wired, but the worker's normal path never reaches it anymore.
- DEC-175 [v0.41]: `POST /v1/emails/batch` reuses the EXACT single-send acceptance pipeline per item — `emailHandler.handleSend`'s entire template/build/authorize+sign/suppression/idempotency/abuse/FileStore/InsertMessage sequence was extracted, unchanged in ordering or behavior, into a new `acceptOne(ctx, tenantID, req, idemKey, now, scheduledAt) (email, replayed bool, *apiError)` method that both `handleSend` and the new `handleSendBatch` call. `authorizeAndSign` and `checkRecipientsForAcceptance` were changed to return `*apiError` instead of writing directly to `http.ResponseWriter` (previously safe only because there was exactly one caller per request; a batch caller must be able to attribute a failure to one item without corrupting the shared HTTP response). The full pre-existing `email_handler_test.go`/`idempotency_test.go` suite (109 tests) was run unchanged after the refactor and passed with zero modifications, confirming byte-for-byte behavioral equivalence for the single-send path.
- DEC-176 [v0.41]: batch items are fully independent, both transactionally and for abuse-control accounting — NOT one big transaction, NOT a shared abuse-control budget. Each item calls `admitSend` independently (the same call `acceptOne` already made for a single send), so a batch of N items can never buy a tenant more sending throughput than N individual `POST /v1/emails` calls would have gotten; the tenant queue cap and recipient rate limit still bite mid-batch (proven by `TestBatchSendAbuseControlsAppliedPerItemIndependently`). PARTIAL FAILURE, never all-or-nothing: one item's rejection (validation, suppression, domain-auth, abuse-control, or idempotency conflict) never blocks its siblings — every item gets exactly one result at its own index, in request order.
- DEC-177 [v0.41]: idempotency is per-item, not per-batch-request. There is no batch-level `Idempotency-Key` header (that HTTP mechanism is inherently single-valued per request, and batch items are independent messages, unlike v0.36 Broadcasts' one-template-many-recipients shape); instead each item MAY carry an optional `idempotency_key` JSON field, reusing the exact same `idempotency.Fingerprint`/`ClaimIdempotencyKey`/replay machinery `POST /v1/emails`' header already drives (via the shared `acceptOne`). A malformed per-item key rejects the WHOLE batch (400) before any item is processed — same "fail fast on cheap, structural problems before touching the database" posture as the existing single-send path.
- DEC-178 [v0.41]: `MaxBatchSize = 100`, chosen as a round number consistent with this package's existing bounds (`defaultMaxRecipients=50` per message, `maxLimit=100` for pagination), not tied to any per-tenant plan/tier concept since none exists yet in this codebase. An oversized (or empty) batch is rejected as a whole, BEFORE any item is processed (verified by asserting zero messages exist after a rejected oversized-batch request) — never a partial batch truncated silently to the cap.
- DEC-179 [PR #19 review, Strix LOW/CWE-400]: `requestLimitMiddleware` charges exactly Cost:1 against the tenant/API-key request buckets for the WHOLE HTTP request, regardless of body size — correct for a single-item send, but it let one token buy up to 100x the CPU/DB work (DKIM RSA signing, verified-sender lookup, template render, suppression query) of a normal send for a 100-item batch. Fixed in `handleSendBatch` itself: after decoding and bounds-checking the batch (so `len(req.Emails)` is known) but BEFORE any per-item work runs, it charges the SAME `TenantRequestKey`/`APIKeyGuardKey` buckets the middleware already used for the remaining `N-1` tokens a batch this size actually costs. A refusal here rejects the whole batch (429) with zero items touched — same "reject before doing expensive work" posture as every other abuse-control check in this package. A single-item batch charges nothing extra (the middleware's existing charge is already correct for it).
- DEC-180 [PR #19 review, Greptile P1]: `acceptOne`'s idempotency-poll path (`pollIdempotencyCompletion`) can block up to `idempotencyPollTimeout` (5s) per item when that item's key is already claimed in-progress elsewhere; called strictly sequentially across up to 100 batch items, a run of unlucky items could take minutes and have the connection closed by `writeTimeout` (15s) mid-response, silently leaving later items unprocessed despite the endpoint's one-result-per-item contract. Fixed with a batch-wide deadline: `emailHandler.batchBudget` (a field, defaulted to the `batchProcessingBudget` constant of 10s in `newEmailHandler`, exactly like the existing `now` field — so tests can inject a near-zero budget and exercise the timeout path deterministically instead of waiting for a real multi-second stall) bounds the WHOLE per-item loop via `context.WithTimeout`. Once spent, every remaining item gets an explicit `batch_processing_timeout` result (never silently dropped) instead of being attempted.
- DEC-181 [v0.42]: SMTP submission uses IMPLICIT TLS (SMTPS-style, TLS from connection start) via `crypto/tls.Listen`, not STARTTLS mid-connection upgrade. Simpler and lower-risk than restructuring the existing `internal/smtp` connection/reader state machine to support a buffered-reader reset after a STARTTLS command, and RFC 8314 explicitly recommends implicit TLS for submission. `MAILX_SUBMISSION_ADDR`/`_TLS_CERT`/`_TLS_KEY` all required together; if any is unset the submission component stays running (blocks on ctx, matching every other long-running component) but binds no listener — never falls back to plaintext.
- DEC-182 [v0.42]: AUTH PLAIN only (RFC 4954), inline form (`AUTH PLAIN <base64>`), no AUTH LOGIN and no multi-line challenge form. Credential = the full MailX API key as the password field; the username field is accepted but not checked against anything (this system has no separate username concept). `internal/smtp.Config` gained `RequireAuth bool` + `Authenticator func(user, pass string) (tenantID string, ok bool)`; MAIL FROM is refused with 530 until AUTH succeeds when `RequireAuth` is set. The pre-existing unauthenticated dev-receiver path (`runSMTPReceiver`, port 2525) is untouched — `RequireAuth` defaults false.
- DEC-183 [v0.42]: submitted mail reuses the EXACT `POST /v1/emails` acceptance pipeline via a new `api.SubmissionAcceptor` (wraps the unexported `emailHandler`/`acceptOne`) — same domain verification, DKIM signing, suppression check, abuse controls, durable InsertMessage. No second accept pipeline. From/To/Cc/Bcc/Subject/Text/HTML come from `mail.ParseMessage`'s already-parsed header fields; Reply-To and per-item idempotency are not exposed over SMTP submission (out of scope, matches the milestone's minimal-surface instruction).
- DEC-184 [v0.42, caught by live smoke test before commit]: the initial `runSubmissionReceiver` returned `nil` immediately when disabled (unset `MAILX_SUBMISSION_ADDR`/cert/key), which `runComponents` treats as "exited unexpectedly" and uses to cancel every other component — an unconfigured submission listener took down the entire server. Fixed to block on `<-ctx.Done()` when disabled, matching every other long-running component's contract. Caught by a real end-to-end run (Docker boot logs), not by the unit test suite, which only exercises the `internal/smtp` package directly and never boots `cmd/mailx`.
- DEC-185 [v0.43]: opt-in per-send only (`track_opens`/`track_clicks` booleans on `sendEmailRequest`), off by default, no template-level default — matches v0.38's deliberate open/click deferral reasoning (DEC-155, already in Ilana) about false-positive risk; the milestone's own scope only required opt-in, not a second per-template flag surface. Reuses the existing `events` table with two new event types (`opened`,`clicked`, migration 000024's CHECK constraint update) — no new aggregate table, matching v0.38's posture.
- DEC-186 [v0.43]: no durable per-link/per-open table. Pixel and rewritten-link URLs carry a self-contained HMAC-signed token (`internal/tracking.Sign`/`Verify`: base64url(JSON payload) + "." + base64url(HMAC-SHA256)) encoding tenant/message/recipient/(destination URL for clicks). The redirect endpoint's destination therefore can NEVER be attacker-controlled (open-redirect-proof by construction: MailX itself signed the exact URL at send time) without needing a new table or an allow-list. Requires `MAILX_TRACKING_SECRET` (HMAC key) and `MAILX_TRACKING_BASE_URL`; either unset silently no-ops the feature (an opted-in send just gets its HTML unmodified) rather than emitting broken links.
- DEC-187 [v0.43]: link rewriting is a regexp over `href="https?://..."` (not a full HTML parser) — proportionate to the milestone's scope; a link containing "unsubscribe" (case-insensitive) anywhere in its URL is never rewritten. MailX does not yet generate its own unsubscribe links/List-Unsubscribe headers (confirmed via source-truth grep — no such feature exists), so this is the only compliance-link signal available; a future unsubscribe-link feature should reuse the same exclusion.
- DEC-188 [v0.43]: attribution recipient is `req.To[0]` (the first To address) — MailX's regular send path renders ONE body for potentially multiple RCPT (unlike v0.36 Broadcasts' per-recipient rendering), so exact per-recipient open/click attribution isn't resolvable from a shared body; this is a documented simplification, not a claim of per-recipient accuracy.
- DEC-189 [v0.43]: `email.opened`/`email.clicked` were added to `database.PublicEventType` (so they appear in `GET /v1/events` like every other public event), but were deliberately NOT added to `internal/webhook`'s separate event-type vocabulary/subscription filtering — out of scope for this milestone's minimal-surface instruction; webhook fanout already tolerates them harmlessly (an unmapped-webhook-type is a no-op path, not an error), so this is a safe, explicit deferral rather than a silent gap.
- DEC-190 [v0.44]: SDK code lives in `sdk/node` and `sdk/python` at repo root — separate from the Go module, no Go build/test coupling. Both are thin clients: a generic request method (auth header, JSON, retry on 429/5xx honoring `Retry-After` with exponential backoff+jitter, capped at `max_retries`=3 default), typed methods per OpenAPI resource. `sendEmail`/`sendBatch`/`getEmail`/`listEmails` are fully typed in the Node SDK (hand-mirrored from `SendEmailRequest`/`Email`/`BatchSendRequest`/`BatchSendResponse` in `internal/api`, not code-generated); every other resource (domains, DKIM/SPF/DMARC/BIMI, webhooks, templates, contacts, audiences, broadcasts, analytics, suppressions, events) has a typed method signature with a loosely-typed JSON body/result — proportionate to budget, not a JSON-Schema-to-TS/Python codegen pipeline.
- DEC-191 [v0.44]: React Email integration needs no SDK code at all — `@react-email/render`'s output is already the exact `html`/`text` shape `sendEmail` accepts, so it's a README usage pattern, not a bundled dependency or wrapper function. Contract tests (`test/contract.test.js`, `tests/test_contract.py`) run against a REAL MailX server via `MAILX_SDK_TEST_BASE_URL`/`MAILX_SDK_TEST_API_KEY` env vars and skip cleanly (verified) when unset — not executed against a live server this session (Docker was unavailable), but unit tests (which are the correctness signal for an HTTP client library — request shape, auth header, retry behavior, error mapping) all pass in both SDKs.
- DEC-192 [v0.44]: added `sdk/go` as a third official SDK, separate Go module `github.com/Ferousco-dev/mailx-go` (go 1.21) with zero build/test coupling to the main `github.com/Ferousco-dev/mailx` server module — same pattern as DEC-190. `SendEmail`/`SendBatch`/`GetEmail`/`ListEmails` are fully typed (structs mirror `internal/api/email_types.go`/`email_batch.go` field names and JSON tags); all other resources return `mailx.JSON` (`map[string]any`) with typed method signatures. Retry core matches Node/Python: 429/5xx retried up to `WithMaxRetries` (default 3), honors `Retry-After`, else exponential backoff+jitter. `go build`/`go vet`/`go test` all pass; live contract test (`contract_test.go`) gated on `MAILX_SDK_TEST_BASE_URL`/`MAILX_SDK_TEST_API_KEY`, verified to skip cleanly, not run live (Docker unavailable this session, same as Node/Python).
- DEC-193 [v0.44]: added `sdk/php` (`mailx/sdk` Composer package, PHP >=8.1) and `sdk/ruby` (`mailx-sdk` gem, Ruby >=2.6) as a fourth and fifth official SDK — same design as Node/Python/Go: one generic retrying request core (429/5xx retried up to a configurable max, default 3, honoring `Retry-After` else exponential backoff+jitter) plus one method per OpenAPI resource, array/hash-in-array/hash-out (no hand-typed request/response classes — proportionate to budget, matching the "core typed, rest loose" posture already set for the other three SDKs, except here even the core methods are untyped arrays/hashes since PHP/Ruby don't get the same value from static structs that TS/Go do). PHP uses a `Transport` interface (`CurlTransport` default) so tests inject a fake transport instead of hitting the network; Ruby's `Client` takes an optional `http_client` callable for the same reason. Renamed PHP's error property from `code` to `apiCode` — `\Exception::$code` already exists in the parent class and a typed re-declaration is a fatal PHP error, caught by `php -l` during this session. 4 unit tests pass in each (PHPUnit / Minitest, both stdlib-adjacent, no new runtime dependency beyond the test framework itself); gated contract tests (`MAILX_SDK_TEST_BASE_URL`/`_API_KEY`) verified to skip cleanly in both, not run live (Docker unavailable, same deferral as the other three SDKs). Go/Node/Python sides confirmed untouched via `git status` before commit.
- DEC-194 [v0.44]: added `.github/workflows/sdk-release.yml` — on every push to `main`, tars each `sdk/<lang>` directory and publishes a GitHub Release (tag `sdk-YYYY.MM.DD-<short-sha>`) with the assets attached. The release body explicitly states the SDKs are under active development, APIs may change without notice, and none are published to a package registry (npm/PyPI/pkg.go.dev/Packagist/RubyGems) yet — this is a snapshot artifact for early testers, not a versioned/supported release. Chosen over registry publishing because none of the five SDKs have been reviewed for a 1.0 API contract yet; registry publishing is a separate, later decision.
